RewriteEngine On

# Canonical: redirect www to non-www and enforce HTTPS in one step
RewriteCond %{HTTP_HOST} ^www\.ekalliptus\.id$ [OR]
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://ekalliptus.id%{REQUEST_URI} [R=301,L]