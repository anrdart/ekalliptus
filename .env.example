# EmailJS Configuration
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
VITE_EMAILJS_RECEIVER=

# API Configuration
# Base URL for your backend API (if using actual backend)
VITE_API_BASE_URL=http://localhost:3000/api

# Application Configuration
VITE_APP_NAME=Ekalliptus
VITE_APP_URL=https://ekalliptus.id

# UI Configuration
VITE_DEFAULT_CURRENCY=IDR
VITE_DEFAULT_LOCALE=id-ID

# Logging Configuration
VITE_ENABLE_DEBUG_MODE=false
VITE_ENABLE_API_LOGGING=true

# Feature Flags
VITE_ENABLE_RATE_LIMITING=true

# Security Configuration
VITE_ENABLE_SECURITY_MIDDLEWARE=true
VITE_CORS_ORIGINS=https://ekalliptus.id,https://www.ekalliptus.id

# Development Configuration
VITE_PORT=5173
VITE_ENABLE_MOCK_DATA=true

# Supabase Configuration
# Project Name: ekalliptus
# Project ID: muyzxygtlwsfegzyvgcm
# Get these values from: https://app.supabase.com/project/muyzxygtlwsfegzyvgcm/settings/api

# Supabase URL (public, safe to expose in frontend)
VITE_SUPABASE_URL=https://muyzxygtlwsfegzyvgcm.supabase.co

# Supabase Anon Key (public, safe to expose in frontend)
# This is the 'anon' public key from Supabase
VITE_SUPABASE_ANON_KEY=

# Supabase Service Role Key (PRIVATE - server-side only!)
# WARNING: Never expose this in frontend code or commit to version control
# This is the 'service_role' key from Supabase - use only in secure server environments
SUPABASE_SERVICE_ROLE_KEY=

# Database Configuration
VITE_SUPABASE_DB_SCHEMA=public

# Real-time Configuration
VITE_SUPABASE_REALTIME_EVENTS_PER_SECOND=10

# Rate Limiting
# Maximum API requests per minute per user
VITE_API_RATE_LIMIT=60
