---
import Layout from '../layouts/Layout.astro'
import Hero from '../components/Hero.astro'
import Services from '../components/Services.astro'
import FAQ from '../components/FAQ.astro'
import ContactCTA from '../components/ContactCTA.astro'
import ConsultationDialog from '../components/ConsultationDialog.astro'
import Navigation from '../components/Navigation.astro'
import VantaBackground from '../components/VantaBackground.astro'
import PreLoader from '../components/PreLoader.astro'
import JsonLd from '../components/JsonLd.astro'
import { t, getLocaleFromRequest, getDir } from '../i18n'
import { Sparkles, Info, Target, Layers, Check, Shield, Lock, FileText, ArrowRight, Zap, CheckCircle, Users } from 'lucide-astro'

const locale = getLocaleFromRequest(Astro.request)
const dir = getDir(locale)

const words = [
  t('hero.uiUxDesign', locale),
  t('hero.digitalExperience', locale),
  t('hero.web3d', locale),
  t('hero.branding', locale)
]

const FAQ_COUNT = 8
const faqItems = []
for (let i = 0; i < FAQ_COUNT; i++) {
  const question = t(`faq.items[${i}].question`)
  const answer = t(`faq.items[${i}].answer`)
  if (question && !question.includes('faq.items[')) {
    faqItems.push({ question, answer })
  }
}
---

<Layout
  title="Ekalliptus Digital | Jasa Website & Aplikasi Mobile Terbaik Indonesia"
  description="Jasa pembuatan website profesional, aplikasi mobile, WordPress & UI/UX design. Berbasis Tegal, melayani seluruh Indonesia. Konsultasi GRATIS!"
  lang={locale}
  dir={dir}
  keywords="jasa pembuatan website, jasa aplikasi mobile, web developer Indonesia, digital agency Tegal, jasa wordpress, ui ux design Indonesia"
>
  <JsonLd locale={locale} page="home" faqItems={faqItems} />
  <VantaBackground />
  <PreLoader />
  <Navigation t={(key: string) => t(key, locale)} currentLang={locale} />
  
  <main class="relative z-0 flex min-h-screen flex-col pt-14 sm:pt-16 md:pt-20">
    <div class="relative flex flex-col gap-16 sm:gap-24 pb-16 sm:pb-24">
      <Hero t={(key: string) => t(key, locale)} />
      
      <div class="text-center py-6 sm:py-8 px-4">
        <div class="inline-flex items-center gap-2 rounded-full bg-card/20 px-4 sm:px-5 py-2 text-[0.65rem] sm:text-xs uppercase tracking-[0.3em] sm:tracking-[0.35em] text-foreground/70 shadow-lg shadow-primary/10 mb-4 sm:mb-6">
          <Sparkles class="h-3.5 w-3.5 sm:h-4 sm:w-4" />
          <span>{t('hero.solusiDigital', locale)}</span>
        </div>
        
        <p class="text-base sm:text-lg leading-relaxed text-foreground/70 md:text-xl max-w-3xl mx-auto px-2">
          {t('hero.description', locale)}
        </p>
        
        <p class="mt-2 sm:mt-3 text-sm sm:text-base leading-relaxed text-primary font-medium md:text-lg max-w-2xl mx-auto px-2">
          {t('hero.subdescription', locale)}
        </p>
        
        <p class="mt-3 sm:mt-4 text-xs sm:text-sm font-medium uppercase tracking-[0.35em] sm:tracking-[0.4em] text-foreground/50">
          {t('hero.fokus', locale)}
          <span id="typewriter" class="ml-2 sm:ml-3 inline-block rounded-full bg-card/20 px-2.5 sm:px-3 py-1 text-[0.65rem] sm:text-xs font-semibold uppercase tracking-[0.15em] sm:tracking-[0.2em] text-foreground align-middle">
            {words[0]}
          </span>
        </p>
      </div>
      
      <section id="application-purpose" class="content-vis relative px-4 py-12 sm:py-16 bg-card/5">
        <div class="relative z-10 mx-auto max-w-5xl">
          <div class="mx-auto mb-8 sm:mb-12 max-w-4xl text-center">
            <div class="inline-flex items-center gap-2 rounded-full bg-primary/10 px-4 sm:px-5 py-2 text-[0.6rem] sm:text-[0.65rem] font-semibold uppercase tracking-[0.5em] sm:tracking-[0.6em] text-primary mb-4 sm:mb-6">
              <Info class="h-3.5 w-3.5 sm:h-4 sm:w-4" />
              <span>{t('appPurpose.badge', locale)}</span>
            </div>
            
            <h2 class="text-2xl sm:text-3xl font-semibold text-foreground md:text-4xl mb-4 sm:mb-6">
              {t('appPurpose.title', locale)}
            </h2>
            
            <p class="text-base sm:text-lg text-muted-foreground leading-relaxed max-w-3xl mx-auto mb-6 sm:mb-8">
              {t('appPurpose.description', locale)}
            </p>
          </div>
            
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6 mb-8 sm:mb-12">
            <div class="glass-panel rounded-2xl p-5 sm:p-6 bg-card/10 border border-border/20">
              <div class="rounded-full bg-primary/10 p-3 w-fit mb-3 sm:mb-4">
                <Target class="h-5 w-5 sm:h-6 sm:w-6 text-primary" />
              </div>
              <h3 class="text-lg sm:text-xl font-semibold text-foreground mb-2 sm:mb-3">{t('appPurpose.whatWeDo.title', locale)}</h3>
              <p class="text-muted-foreground leading-relaxed text-sm">{t('appPurpose.whatWeDo.description', locale)}</p>
            </div>
            
            <div class="glass-panel rounded-2xl p-5 sm:p-6 bg-card/10 border border-border/20">
              <div class="rounded-full bg-primary/10 p-3 w-fit mb-3 sm:mb-4">
                <Layers class="h-5 w-5 sm:h-6 sm:w-6 text-primary" />
              </div>
              <h3 class="text-lg sm:text-xl font-semibold text-foreground mb-2 sm:mb-3">{t('appPurpose.ourServices.title', locale)}</h3>
              <ul class="text-muted-foreground text-sm space-y-1">
                <li>{t('appPurpose.ourServices.service1', locale)}</li>
                <li>{t('appPurpose.ourServices.service2', locale)}</li>
                <li>{t('appPurpose.ourServices.service3', locale)}</li>
                <li>{t('appPurpose.ourServices.service4', locale)}</li>
              </ul>
            </div>
            
            <div class="glass-panel rounded-2xl p-5 sm:p-6 bg-card/10 border border-border/20">
              <div class="rounded-full bg-accent/10 p-3 w-fit mb-3 sm:mb-4">
                <Shield class="h-5 w-5 sm:h-6 sm:w-6 text-accent" />
              </div>
              <h3 class="text-lg sm:text-xl font-semibold text-foreground mb-2 sm:mb-3">{t('appPurpose.dataUsage.title', locale)}</h3>
              <p class="text-muted-foreground leading-relaxed text-sm">{t('appPurpose.dataUsage.description', locale)}</p>
            </div>
          </div>
            
          <div class="glass-panel neon-border rounded-2xl sm:rounded-3xl p-5 sm:p-8 bg-card/5">
            <h3 class="text-xl sm:text-2xl font-semibold text-foreground mb-4 sm:mb-6 text-center">{t('appPurpose.howItWorks.title', locale)}</h3>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
              <div>
                <h4 class="text-lg font-semibold text-foreground mb-4">{t('appPurpose.howItWorks.orderProcess.title', locale)}</h4>
                <ol class="space-y-3">
                  <li class="flex gap-3 text-sm text-muted-foreground">
                    <span class="flex-shrink-0 w-6 h-6 rounded-full bg-primary/10 text-primary text-xs flex items-center justify-center font-medium">1</span>
                    {t('appPurpose.howItWorks.orderProcess.step1', locale)}
                  </li>
                  <li class="flex gap-3 text-sm text-muted-foreground">
                    <span class="flex-shrink-0 w-6 h-6 rounded-full bg-primary/10 text-primary text-xs flex items-center justify-center font-medium">2</span>
                    {t('appPurpose.howItWorks.orderProcess.step2', locale)}
                  </li>
                  <li class="flex gap-3 text-sm text-muted-foreground">
                    <span class="flex-shrink-0 w-6 h-6 rounded-full bg-primary/10 text-primary text-xs flex items-center justify-center font-medium">3</span>
                    {t('appPurpose.howItWorks.orderProcess.step3', locale)}
                  </li>
                  <li class="flex gap-3 text-sm text-muted-foreground">
                    <span class="flex-shrink-0 w-6 h-6 rounded-full bg-primary/10 text-primary text-xs flex items-center justify-center font-medium">4</span>
                    {t('appPurpose.howItWorks.orderProcess.step4', locale)}
                  </li>
                </ol>
              </div>
              
              <div>
                <h4 class="text-lg font-semibold text-foreground mb-4">{t('appPurpose.howItWorks.privacyCommitment.title', locale)}</h4>
                <ul class="space-y-3">
                  <li class="flex gap-3 text-sm text-muted-foreground">
                    <Check class="flex-shrink-0 h-5 w-5 text-primary" />
                    {t('appPurpose.howItWorks.privacyCommitment.point1', locale)}
                  </li>
                  <li class="flex gap-3 text-sm text-muted-foreground">
                    <Check class="flex-shrink-0 h-5 w-5 text-primary" />
                    {t('appPurpose.howItWorks.privacyCommitment.point2', locale)}
                  </li>
                  <li class="flex gap-3 text-sm text-muted-foreground">
                    <Check class="flex-shrink-0 h-5 w-5 text-primary" />
                    {t('appPurpose.howItWorks.privacyCommitment.point3', locale)}
                  </li>
                </ul>
              </div>
            </div>
          </div>
          
          <div class="mt-6 sm:mt-8 text-center">
            <p class="text-xs sm:text-sm text-muted-foreground">{t('appPurpose.learnMore', locale)}</p>
            <div class="flex gap-4 justify-center mt-3">
              <a href="/privacy-policy" class="text-sm text-primary hover:underline">{t('privacyPolicy.title', locale)}</a>
              <a href="/terms-of-service" class="text-sm text-primary hover:underline">{t('termsOfService.title', locale)}</a>
            </div>
          </div>
        </div>
      </section>
      
      <Services t={(key: string) => t(key, locale)} />
      <FAQ t={(key: string) => t(key, locale)} />
      <ContactCTA t={(key: string) => t(key, locale)} />
      <ConsultationDialog t={(key: string) => t(key, locale)} />

      <!-- Blog Section -->
      <section id="blog" class="px-4 py-12 sm:py-16 bg-card/5">
        <div class="mx-auto max-w-6xl">
          <div class="text-center mb-8 sm:mb-12">
            <div class="inline-flex items-center gap-2 rounded-full bg-primary/10 px-4 sm:px-5 py-2 text-[0.6rem] sm:text-[0.65rem] font-semibold uppercase tracking-[0.5em] sm:tracking-[0.6em] text-primary mb-4">
              <span>📝</span>
              <span>{t('blog.badge', locale)}</span>
            </div>

            <h2 class="text-2xl sm:text-3xl font-semibold text-foreground mb-4">
              {t('blog.title', locale)}
            </h2>

            <p class="text-base sm:text-lg text-muted-foreground max-w-2xl mx-auto">
              {t('blog.description', locale)}
            </p>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-6 sm:gap-8 mb-8">
            <!-- Blog Post 1 -->
            <a href="/blog/jasa-pembuatan-website-tegal" class="group rounded-2xl glass-panel bg-card/10 border border-border/20 overflow-hidden transition-all hover:border-primary/30 hover:shadow-lg hover:shadow-primary/5">
              <div class="aspect-video overflow-hidden bg-card/30 skel-shimmer">
                <img src="/blog/jasa-website-tegal.svg" alt="Jasa Pembuatan Website Tegal" width="640" height="360" class="img-skel w-full h-full object-cover transition-transform duration-300 group-hover:scale-105" loading="lazy" decoding="async" onload="this.classList.add('img-loaded')" onerror="this.onerror=null;this.src='/blog/placeholder.svg'" />
              </div>
              <div class="p-5 sm:p-6">
                <span class="rounded-full bg-primary/10 px-3 py-1 text-[0.65rem] font-semibold uppercase tracking-wider text-primary">
                  {t('blog.categories.web-development', locale)}
                </span>
                <h3 class="text-lg sm:text-xl font-semibold text-foreground mt-3 mb-2 group-hover:text-primary transition-colors">
                  Jasa Pembuatan Website Tegal: Harga & Portfolio 2026
                </h3>
                <p class="text-sm text-muted-foreground line-clamp-2 mb-4">
                  Temukan layanan pembuatan website profesional di Tegal dengan harga terjangkau. Mulai dari Rp 1,5 juta untuk company profile.
                </p>
                <span class="inline-flex items-center gap-2 text-sm font-medium text-primary">
                  {t('blog.readMore', locale)}
                  <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
                </span>
              </div>
            </a>

            <!-- Blog Post 2 -->
            <a href="/blog/biaya-pembuatan-aplikasi-android-ios" class="group rounded-2xl glass-panel bg-card/10 border border-border/20 overflow-hidden transition-all hover:border-primary/30 hover:shadow-lg hover:shadow-primary/5">
              <div class="aspect-video overflow-hidden bg-card/30 skel-shimmer">
                <img src="/blog/biaya-aplikasi-mobile.svg" alt="Biaya Pembuatan Aplikasi Mobile" width="640" height="360" class="img-skel w-full h-full object-cover transition-transform duration-300 group-hover:scale-105" loading="lazy" decoding="async" onload="this.classList.add('img-loaded')" onerror="this.onerror=null;this.src='/blog/placeholder.svg'" />
              </div>
              <div class="p-5 sm:p-6">
                <span class="rounded-full bg-accent/10 px-3 py-1 text-[0.65rem] font-semibold uppercase tracking-wider text-accent">
                  {t('blog.categories.mobile-app', locale)}
                </span>
                <h3 class="text-lg sm:text-xl font-semibold text-foreground mt-3 mb-2 group-hover:text-primary transition-colors">
                  Biaya Pembuatan Aplikasi Android & iOS di Indonesia
                </h3>
                <p class="text-sm text-muted-foreground line-clamp-2 mb-4">
                  Panduan lengkap estimasi biaya pembuatan aplikasi mobile cross-platform dengan React Native dan Flutter.
                </p>
                <span class="inline-flex items-center gap-2 text-sm font-medium text-primary">
                  {t('blog.readMore', locale)}
                  <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
                </span>
              </div>
            </a>

            <!-- Blog Post 3 -->
            <a href="/blog/wordpress-custom-vs-template" class="group rounded-2xl glass-panel bg-card/10 border border-border/20 overflow-hidden transition-all hover:border-primary/30 hover:shadow-lg hover:shadow-primary/5">
              <div class="aspect-video overflow-hidden bg-card/30 skel-shimmer">
                <img src="/blog/wordpress-custom-vs-template.svg" alt="WordPress Custom vs Template" width="640" height="360" class="img-skel w-full h-full object-cover transition-transform duration-300 group-hover:scale-105" loading="lazy" decoding="async" onload="this.classList.add('img-loaded')" onerror="this.onerror=null;this.src='/blog/placeholder.svg'" />
              </div>
              <div class="p-5 sm:p-6">
                <span class="rounded-full bg-primary/10 px-3 py-1 text-[0.65rem] font-semibold uppercase tracking-wider text-primary">
                  {t('blog.categories.wordpress', locale)}
                </span>
                <h3 class="text-lg sm:text-xl font-semibold text-foreground mt-3 mb-2 group-hover:text-primary transition-colors">
                  WordPress Custom vs Template: Mana yang Lebih Baik?
                </h3>
                <p class="text-sm text-muted-foreground line-clamp-2 mb-4">
                  Perbandingan lengkap antara WordPress custom development dengan tema premium. Pilih sesuai kebutuhan bisnis Anda.
                </p>
                <span class="inline-flex items-center gap-2 text-sm font-medium text-primary">
                  {t('blog.readMore', locale)}
                  <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
                </span>
              </div>
            </a>
          </div>

          <div class="text-center">
            <a
              href="/blog"
              class="inline-flex items-center gap-2 rounded-full bg-primary px-6 py-3 text-sm font-semibold text-primary-foreground transition hover:bg-primary/90"
            >
              {t('blog.readMore', locale)}
              <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
            </a>
          </div>
        </div>
      </section>
      
      <footer id="about" class="mt-8 sm:mt-12 border-t border-border/20 bg-card/5 py-8 sm:py-12 safe-bottom">
        <div class="mx-auto max-w-6xl px-4">
          <div class="grid grid-cols-1 gap-6 sm:gap-8 md:grid-cols-3">
            <div>
              <div class="flex items-center gap-3 mb-3 sm:mb-4">
                <img src="/ekalliptus_rounded.webp" alt="Ekalliptus" width="40" height="40" class="h-9 w-auto sm:h-10" loading="lazy" decoding="async" />
                <span class="font-semibold text-base sm:text-lg text-foreground">Ekalliptus</span>
              </div>
              <p class="text-sm text-muted-foreground">
                {t('index.about.philosophyDesc', locale)}
              </p>
            </div>
            
            <div>
              <h3 class="font-semibold text-foreground mb-4">{t('services.title', locale)}</h3>
              <ul class="space-y-2 text-sm text-muted-foreground">
                <li>{t('services.cards.websiteDevelopment.title', locale)}</li>
                <li>{t('services.cards.mobileAppDevelopment.title', locale)}</li>
                <li>{t('services.cards.wordpressDevelopment.title', locale)}</li>
                <li>{t('services.cards.photoVideoEditing.title', locale)}</li>
              </ul>
            </div>
            
            <div>
              <h3 class="font-semibold text-foreground mb-4">{t('footer.contact', locale)}</h3>
              <ul class="space-y-2 text-sm text-muted-foreground">
                <li>ekalliptus@gmail.com</li>
                <li>+62 819-9990-0306</li>
                <li>Tegal, Indonesia</li>
              </ul>
            </div>
          </div>
          
          <div class="mt-8 pt-8 border-t border-border/20 flex flex-col md:flex-row justify-between items-center gap-4">
            <p class="text-sm text-muted-foreground">
              © {new Date().getFullYear()} Ekalliptus Digital. All rights reserved.
            </p>
            <div class="flex gap-4">
              <a href="/privacy-policy" class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                {t('privacyPolicy.title', locale)}
              </a>
              <a href="/terms-of-service" class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                {t('termsOfService.title', locale)}
              </a>
            </div>
          </div>
        </div>
      </footer>
    </div>
  </main>
</Layout>

<script define:vars={{ words }}>
  const typewriterEl = document.getElementById('typewriter')
  let currentWordIndex = 0
  let charIndex = 0
  let isDeleting = false
  
  function typeWriter() {
    if (!typewriterEl) return
    
    const currentWord = words[currentWordIndex]
    
    if (isDeleting) {
      typewriterEl.textContent = currentWord.substring(0, charIndex - 1)
      charIndex--
    } else {
      typewriterEl.textContent = currentWord.substring(0, charIndex + 1)
      charIndex++
    }
    
    let timeout = isDeleting ? 40 : 80
    
    if (!isDeleting && charIndex === currentWord.length) {
      timeout = 1400
      isDeleting = true
    } else if (isDeleting && charIndex === 0) {
      isDeleting = false
      currentWordIndex = (currentWordIndex + 1) % words.length
      timeout = 500
    }
    
    setTimeout(typeWriter, timeout)
  }
  
  setTimeout(typeWriter, 1000)
</script>
