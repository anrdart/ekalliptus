# Admin CRM Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transform the existing Astro admin dashboard into a complete CRM with auth, customer management, sales pipeline, analytics, and content management.

**Architecture:** Astro v6 SSR + Supabase + Cloudflare Workers. Admin routes guarded by Astro middleware that validates a Supabase Auth session cookie. Pages render server-side; small inline scripts handle interactivity (charts, polling, kanban drag). New tables `leads` and `activities` introduced in Phase 2; Phase 1 derives customers from existing `orders`.

**Tech Stack:** Astro 6, TypeScript, Supabase JS v2, Tailwind CSS, ApexCharts (lazy CDN), Vitest, Bun.

**Spec:** [docs/superpowers/specs/2026-05-05-admin-crm-design.md](../specs/2026-05-05-admin-crm-design.md)

---

## Conventions used throughout

- All file paths are absolute from repo root (e.g. `src/middleware.ts`).
- Run tests with `bun run test` (added in Task 0.1). Run dev server with `bun run dev`.
- Each task ends with a commit. Commit messages follow Conventional Commits.
- After every task, run `bun run astro check` to ensure type safety. (Already an installed dependency.)
- Subagents/engineers: read the spec section referenced by each Phase before starting that phase.

---

## PHASE 0 — Project bootstrap

### Task 0.1: Add `test` script and create test directory layout

**Files:**
- Modify: `package.json` (scripts section)
- Create: `src/__tests__/.gitkeep`

- [ ] **Step 1:** Open `package.json` and add a `test` script. The Edit:

```json
"scripts": {
    "dev": "astro dev",
    "build": "astro build && node -e \"const fs=require('fs'),path=require('path');const s='dist/server',c='dist/client';fs.copyFileSync(path.join(s,'entry.mjs'),path.join(c,'_worker.js'));fs.cpSync(path.join(s,'chunks'),path.join(c,'chunks'),{recursive:true});fs.copyFileSync(path.join(s,'virtual_astro_middleware.mjs'),path.join(c,'virtual_astro_middleware.mjs'));fs.rmSync('.wrangler/deploy',{recursive:true,force:true})\"",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate:blog": "bun run scripts/migrate-blog-to-supabase.ts"
},
```

- [ ] **Step 2:** Create directory marker:

```bash
mkdir -p src/__tests__ && touch src/__tests__/.gitkeep
```

- [ ] **Step 3:** Verify Vitest runs:

```bash
bun run test
```

Expected: passes (only existing favicon test runs; "Test Files: 1 passed").

- [ ] **Step 4:** Commit.

```bash
git add package.json src/__tests__/.gitkeep
git commit -m "chore: add test script and admin test directory"
```

---

## PHASE 1 — Foundation (Auth, Real Data, Customers)

> Spec reference: §2 Phase 1, §3.3 Auth, §4.1 Customer derivation, §5.2–5.5

### Task 1.1: Auth helper module — types and `getSession`

**Files:**
- Create: `src/lib/admin/auth.ts`
- Test: `src/__tests__/admin-auth.test.ts`

- [ ] **Step 1: Write failing test** for parsing the session cookie name constant.

Write `src/__tests__/admin-auth.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { ADMIN_COOKIE_NAME, parseSessionCookie } from '../lib/admin/auth'

describe('admin auth helpers', () => {
  it('exposes a stable cookie name', () => {
    expect(ADMIN_COOKIE_NAME).toBe('admin_session')
  })

  it('returns null when no cookie header is present', () => {
    expect(parseSessionCookie(null)).toBeNull()
    expect(parseSessionCookie('')).toBeNull()
  })

  it('extracts the admin session value from a cookie header', () => {
    const header = 'theme=dark; admin_session=abc123; lang=id'
    expect(parseSessionCookie(header)).toBe('abc123')
  })

  it('returns null when admin_session is absent', () => {
    expect(parseSessionCookie('theme=dark; lang=id')).toBeNull()
  })
})
```

- [ ] **Step 2: Run test, expect failure**

```bash
bun run test src/__tests__/admin-auth.test.ts
```

Expected: FAIL — module not found.

- [ ] **Step 3: Implement minimal helper**

Write `src/lib/admin/auth.ts`:

```ts
import type { APIContext } from 'astro'
import type { User } from '@supabase/supabase-js'
import { getSupabase } from '../supabase'
import type { UserRole } from '../../types/database'

export const ADMIN_COOKIE_NAME = 'admin_session'

export function parseSessionCookie(header: string | null): string | null {
  if (!header) return null
  const parts = header.split(/;\s*/)
  for (const part of parts) {
    const [name, ...rest] = part.split('=')
    if (name === ADMIN_COOKIE_NAME) return rest.join('=') || null
  }
  return null
}

export interface AdminSession {
  user: User
  role: UserRole
  displayName: string
}

export async function getAdminSession(ctx: APIContext): Promise<AdminSession | null> {
  const token = parseSessionCookie(ctx.request.headers.get('cookie'))
  if (!token) return null

  const supabase = getSupabase(true)
  if (!supabase) return null

  const { data: userData, error } = await supabase.auth.getUser(token)
  if (error || !userData.user) return null

  const { data: profile } = await supabase
    .from('profiles')
    .select('role, display_name')
    .eq('user_id', userData.user.id)
    .single()

  if (!profile) return null

  return {
    user: userData.user,
    role: (profile.role as UserRole) ?? 'admin',
    displayName: profile.display_name
  }
}

export function hasRole(session: AdminSession, allowed: UserRole[]): boolean {
  if (session.role === 'owner') return true
  return allowed.includes(session.role)
}

export async function requireAdmin(ctx: APIContext): Promise<AdminSession | Response> {
  const session = await getAdminSession(ctx)
  if (!session) {
    return new Response(JSON.stringify({ error: 'Unauthorized' }), {
      status: 401,
      headers: { 'Content-Type': 'application/json' }
    })
  }
  return session
}

export async function requireRole(ctx: APIContext, roles: UserRole[]): Promise<AdminSession | Response> {
  const result = await requireAdmin(ctx)
  if (result instanceof Response) return result
  if (!hasRole(result, roles)) {
    return new Response(JSON.stringify({ error: 'Forbidden' }), {
      status: 403,
      headers: { 'Content-Type': 'application/json' }
    })
  }
  return result
}
```

- [ ] **Step 4: Run test, expect pass**

```bash
bun run test src/__tests__/admin-auth.test.ts
```

Expected: PASS — 4 tests.

- [ ] **Step 5: Commit**

```bash
git add src/lib/admin/auth.ts src/__tests__/admin-auth.test.ts
git commit -m "feat(admin): add auth helper with session parsing and role check"
```

---

### Task 1.2: Astro middleware to guard `/admin` and `/api/admin`

**Files:**
- Create: `src/middleware.ts`
- Test: `src/__tests__/middleware.test.ts`

- [ ] **Step 1: Write failing test** for the path-matching predicate.

Write `src/__tests__/middleware.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { isProtectedPath, isLoginPath } from '../middleware'

describe('admin middleware path matching', () => {
  it('protects /admin and /admin/* but not /admin/login', () => {
    expect(isProtectedPath('/admin')).toBe(true)
    expect(isProtectedPath('/admin/orders')).toBe(true)
    expect(isProtectedPath('/admin/customers/123')).toBe(true)
    expect(isProtectedPath('/admin/login')).toBe(false)
  })

  it('protects /api/admin/* but not /api/admin/login', () => {
    expect(isProtectedPath('/api/admin/dashboard')).toBe(true)
    expect(isProtectedPath('/api/admin/login')).toBe(false)
  })

  it('does not protect public routes', () => {
    expect(isProtectedPath('/')).toBe(false)
    expect(isProtectedPath('/blog/post-slug')).toBe(false)
    expect(isProtectedPath('/api/order')).toBe(false)
  })

  it('identifies the login page', () => {
    expect(isLoginPath('/admin/login')).toBe(true)
    expect(isLoginPath('/admin')).toBe(false)
  })
})
```

- [ ] **Step 2: Run, expect FAIL** (module not found).

```bash
bun run test src/__tests__/middleware.test.ts
```

- [ ] **Step 3: Implement middleware**

Write `src/middleware.ts`:

```ts
import { defineMiddleware } from 'astro:middleware'
import { getAdminSession } from './lib/admin/auth'

export function isLoginPath(pathname: string): boolean {
  return pathname === '/admin/login' || pathname === '/api/admin/login'
}

export function isProtectedPath(pathname: string): boolean {
  if (isLoginPath(pathname)) return false
  if (pathname === '/admin' || pathname.startsWith('/admin/')) return true
  if (pathname.startsWith('/api/admin/')) return true
  return false
}

export const onRequest = defineMiddleware(async (ctx, next) => {
  const url = new URL(ctx.request.url)

  if (!isProtectedPath(url.pathname)) {
    return next()
  }

  if (import.meta.env.ADMIN_AUTH_DISABLED === 'true') {
    return next()
  }

  const session = await getAdminSession(ctx)
  if (!session) {
    if (url.pathname.startsWith('/api/')) {
      return new Response(JSON.stringify({ error: 'Unauthorized' }), {
        status: 401,
        headers: { 'Content-Type': 'application/json' }
      })
    }
    const next_url = encodeURIComponent(url.pathname + url.search)
    return ctx.redirect(`/admin/login?next=${next_url}`)
  }

  ctx.locals.adminSession = session
  return next()
})
```

- [ ] **Step 4: Add the type for `Astro.locals.adminSession`**

Edit `src/env.d.ts` (create if missing):

```ts
/// <reference types="astro/client" />

declare namespace App {
  interface Locals {
    adminSession?: import('./lib/admin/auth').AdminSession
  }
}
```

- [ ] **Step 5: Run test, expect PASS**

```bash
bun run test src/__tests__/middleware.test.ts
```

- [ ] **Step 6: Run typecheck**

```bash
bun run astro check
```

Expected: 0 errors.

- [ ] **Step 7: Commit**

```bash
git add src/middleware.ts src/__tests__/middleware.test.ts src/env.d.ts
git commit -m "feat(admin): enforce auth middleware on /admin and /api/admin"
```

---

### Task 1.3: Real login page + `POST /api/admin/login`

**Files:**
- Modify: `src/pages/admin/login.astro`
- Create: `src/pages/api/admin/login.ts`
- Modify: `src/pages/api/admin/logout.ts`

- [ ] **Step 1: Replace login page**

Overwrite `src/pages/admin/login.astro`:

```astro
---
import '../../styles/global.css'
import { LayoutDashboard } from 'lucide-astro'

const next = Astro.url.searchParams.get('next') ?? '/admin'
const error = Astro.url.searchParams.get('error')
---
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Login | Ekalliptus Digital</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
</head>
<body class="min-h-screen bg-background text-foreground flex items-center justify-center p-4">
  <main class="glass-card w-full max-w-sm rounded-2xl p-6">
    <div class="mx-auto mb-4 h-12 w-12 rounded-full bg-primary/10 flex items-center justify-center">
      <LayoutDashboard class="h-6 w-6 text-primary" />
    </div>
    <h1 class="text-xl font-bold text-center mb-1">Admin Login</h1>
    <p class="text-sm text-muted-foreground text-center mb-6">Ekalliptus Digital</p>
    {error && (
      <div class="mb-4 rounded-lg border border-destructive/40 bg-destructive/10 px-3 py-2 text-sm text-destructive">
        {error === 'invalid' ? 'Email atau password salah.' : 'Login gagal. Coba lagi.'}
      </div>
    )}
    <form method="POST" action="/api/admin/login" class="space-y-3">
      <input type="hidden" name="next" value={next} />
      <label class="block">
        <span class="text-xs font-medium text-muted-foreground">Email</span>
        <input
          type="email"
          name="email"
          required
          autocomplete="username"
          class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm"
        />
      </label>
      <label class="block">
        <span class="text-xs font-medium text-muted-foreground">Password</span>
        <input
          type="password"
          name="password"
          required
          autocomplete="current-password"
          class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm"
        />
      </label>
      <button
        type="submit"
        class="inline-flex w-full items-center justify-center rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground hover:bg-primary/90"
      >
        Masuk
      </button>
    </form>
  </main>
</body>
</html>
```

- [ ] **Step 2: Create login API**

Write `src/pages/api/admin/login.ts`:

```ts
import type { APIRoute } from 'astro'
import { getSupabase } from '../../../lib/supabase'
import { ADMIN_COOKIE_NAME } from '../../../lib/admin/auth'

export const POST: APIRoute = async ({ request, redirect }) => {
  const form = await request.formData()
  const email = String(form.get('email') ?? '')
  const password = String(form.get('password') ?? '')
  const next = String(form.get('next') ?? '/admin') || '/admin'

  if (!email || !password) {
    return redirect('/admin/login?error=invalid')
  }

  const supabase = getSupabase(true)
  if (!supabase) {
    return redirect('/admin/login?error=server')
  }

  const { data, error } = await supabase.auth.signInWithPassword({ email, password })
  if (error || !data.session) {
    return redirect('/admin/login?error=invalid')
  }

  const { data: profile } = await supabase
    .from('profiles')
    .select('role')
    .eq('user_id', data.user.id)
    .single()

  if (!profile) {
    return redirect('/admin/login?error=no_profile')
  }

  const safeNext = next.startsWith('/admin') ? next : '/admin'
  const maxAge = data.session.expires_in ?? 3600

  return new Response(null, {
    status: 302,
    headers: {
      Location: safeNext,
      'Set-Cookie': `${ADMIN_COOKIE_NAME}=${data.session.access_token}; Path=/; HttpOnly; SameSite=Lax; Max-Age=${maxAge}; Secure`
    }
  })
}
```

- [ ] **Step 3: Update logout**

Overwrite `src/pages/api/admin/logout.ts`:

```ts
import type { APIRoute } from 'astro'
import { ADMIN_COOKIE_NAME, parseSessionCookie } from '../../../lib/admin/auth'
import { getSupabase } from '../../../lib/supabase'

export const POST: APIRoute = async ({ request }) => {
  const token = parseSessionCookie(request.headers.get('cookie'))
  if (token) {
    const supabase = getSupabase(true)
    if (supabase) {
      await supabase.auth.admin.signOut(token).catch(() => undefined)
    }
  }
  return new Response(JSON.stringify({ success: true }), {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
      'Set-Cookie': `${ADMIN_COOKIE_NAME}=; Path=/; Max-Age=0; HttpOnly; SameSite=Lax; Secure`
    }
  })
}
```

- [ ] **Step 4: Manual smoke test**

Add a Supabase user with role `owner` in the `profiles` table (manual one-time SQL):

```sql
-- Run in Supabase SQL editor (replace email/password by creating user via Auth UI first)
INSERT INTO profiles (user_id, email, display_name, whatsapp, role)
VALUES ('<uuid-from-auth-users>', 'admin@ekalliptus.com', 'Admin', '0800000000', 'owner');
```

Run dev server and verify:

```bash
bun run dev
```

Visit `http://localhost:4321/admin` → expect redirect to `/admin/login`. Submit form → expect redirect back to `/admin`. Click logout → expect redirect to `/admin/login`.

- [ ] **Step 5: Commit**

```bash
git add src/pages/admin/login.astro src/pages/api/admin/login.ts src/pages/api/admin/logout.ts
git commit -m "feat(admin): real login form backed by Supabase Auth"
```

---

### Task 1.4: Shared admin UI components

**Files:**
- Create: `src/components/admin/StatCard.astro`
- Create: `src/components/admin/EmptyState.astro`
- Create: `src/components/admin/Pagination.astro`
- Create: `src/components/admin/SearchFilter.astro`

- [ ] **Step 1: Create `StatCard.astro`**

```astro
---
import type { LucideAstro } from 'lucide-astro'
import { TrendingUp, TrendingDown } from 'lucide-astro'

interface Props {
  icon: any
  iconColorClass?: string
  value: string
  label: string
  change?: string
  trend?: 'up' | 'down' | null
}

const { icon: Icon, iconColorClass = 'text-primary', value, label, change, trend } = Astro.props
---
<div class="glass-card rounded-2xl p-6">
  <div class="flex items-center justify-between mb-4">
    <div class={`h-12 w-12 rounded-xl bg-primary/10 flex items-center justify-center`}>
      <Icon class={`h-6 w-6 ${iconColorClass}`} />
    </div>
    {change && trend && (
      <div class="flex items-center gap-1 text-sm">
        {trend === 'up' ? (
          <>
            <TrendingUp class="h-4 w-4 text-green-500" />
            <span class="text-green-500 font-medium">{change}</span>
          </>
        ) : (
          <>
            <TrendingDown class="h-4 w-4 text-red-500" />
            <span class="text-red-500 font-medium">{change}</span>
          </>
        )}
      </div>
    )}
  </div>
  <h3 class="text-2xl font-bold mb-1">{value}</h3>
  <p class="text-sm text-muted-foreground">{label}</p>
</div>
```

- [ ] **Step 2: Create `EmptyState.astro`**

```astro
---
interface Props {
  icon?: any
  title: string
  description?: string
  ctaLabel?: string
  ctaHref?: string
}

const { icon: Icon, title, description, ctaLabel, ctaHref } = Astro.props
---
<div class="flex flex-col items-center justify-center text-center py-12 px-6">
  {Icon && (
    <div class="h-12 w-12 rounded-full bg-muted/40 flex items-center justify-center mb-4">
      <Icon class="h-6 w-6 text-muted-foreground" />
    </div>
  )}
  <h3 class="text-base font-semibold mb-1">{title}</h3>
  {description && <p class="text-sm text-muted-foreground max-w-sm">{description}</p>}
  {ctaLabel && ctaHref && (
    <a href={ctaHref} class="mt-4 inline-flex items-center rounded-xl bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90">
      {ctaLabel}
    </a>
  )}
</div>
```

- [ ] **Step 3: Create `Pagination.astro`**

```astro
---
import { ChevronLeft, ChevronRight } from 'lucide-astro'

interface Props {
  page: number
  totalPages: number
  baseUrl: string  // includes existing query params except `page`
}

const { page, totalPages, baseUrl } = Astro.props
const sep = baseUrl.includes('?') ? '&' : '?'
const prevUrl = page > 1 ? `${baseUrl}${sep}page=${page - 1}` : null
const nextUrl = page < totalPages ? `${baseUrl}${sep}page=${page + 1}` : null
---
<nav class="flex items-center justify-between mt-4 text-sm" aria-label="Pagination">
  <p class="text-muted-foreground">Halaman {page} dari {totalPages}</p>
  <div class="flex gap-2">
    {prevUrl ? (
      <a href={prevUrl} class="inline-flex items-center gap-1 rounded-lg border border-border px-3 py-1.5 hover:bg-accent">
        <ChevronLeft class="h-4 w-4" /> Sebelumnya
      </a>
    ) : (
      <span class="inline-flex items-center gap-1 rounded-lg border border-border px-3 py-1.5 text-muted-foreground opacity-50">
        <ChevronLeft class="h-4 w-4" /> Sebelumnya
      </span>
    )}
    {nextUrl ? (
      <a href={nextUrl} class="inline-flex items-center gap-1 rounded-lg border border-border px-3 py-1.5 hover:bg-accent">
        Selanjutnya <ChevronRight class="h-4 w-4" />
      </a>
    ) : (
      <span class="inline-flex items-center gap-1 rounded-lg border border-border px-3 py-1.5 text-muted-foreground opacity-50">
        Selanjutnya <ChevronRight class="h-4 w-4" />
      </span>
    )}
  </div>
</nav>
```

- [ ] **Step 4: Create `SearchFilter.astro`**

```astro
---
import { Search } from 'lucide-astro'

interface Props {
  placeholder?: string
  value?: string
  name?: string
  action: string
  filters?: Array<{ name: string; label: string; options: Array<{ value: string; label: string }>; selected?: string }>
}

const { placeholder = 'Cari...', value = '', name = 'q', action, filters = [] } = Astro.props
---
<form method="GET" action={action} class="glass-card rounded-2xl p-4 mb-6">
  <div class="flex flex-col lg:flex-row gap-3">
    <div class="flex-1 relative">
      <Search class="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" />
      <input
        type="text"
        name={name}
        value={value}
        placeholder={placeholder}
        class="w-full rounded-lg border border-border bg-background pl-9 pr-3 py-2 text-sm"
      />
    </div>
    {filters.map((f) => (
      <select name={f.name} class="rounded-lg border border-border bg-background px-3 py-2 text-sm">
        {f.options.map((opt) => (
          <option value={opt.value} selected={opt.value === f.selected}>{opt.label}</option>
        ))}
      </select>
    ))}
    <button type="submit" class="rounded-lg bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90">
      Terapkan
    </button>
  </div>
</form>
```

- [ ] **Step 5: Typecheck**

```bash
bun run astro check
```

- [ ] **Step 6: Commit**

```bash
git add src/components/admin/
git commit -m "feat(admin): add shared UI components (StatCard, EmptyState, Pagination, SearchFilter)"
```

---

### Task 1.5: Customer derivation helper

**Files:**
- Create: `src/lib/admin/customers.ts`
- Test: `src/__tests__/admin-customers.test.ts`

- [ ] **Step 1: Write failing test** for the URL identity encoder.

```ts
import { describe, it, expect } from 'vitest'
import { encodeCustomerId, decodeCustomerId } from '../lib/admin/customers'

describe('customer identity encoding', () => {
  it('round-trips a whatsapp number with +', () => {
    const id = encodeCustomerId('+6281234567890')
    expect(typeof id).toBe('string')
    expect(decodeCustomerId(id)).toBe('+6281234567890')
  })

  it('returns null for empty input', () => {
    expect(encodeCustomerId('')).toBeNull()
    expect(decodeCustomerId('')).toBeNull()
  })

  it('encoding is URL-safe', () => {
    const id = encodeCustomerId('+62 812-3456-7890')
    expect(id).not.toContain(' ')
    expect(id).not.toContain('+')
  })
})
```

- [ ] **Step 2: Run, expect FAIL**

```bash
bun run test src/__tests__/admin-customers.test.ts
```

- [ ] **Step 3: Implement helper**

Write `src/lib/admin/customers.ts`:

```ts
import { getSupabase } from '../supabase'
import type { Order } from '../../types/database'

export interface CustomerSummary {
  id: string                    // url-safe encoded whatsapp
  whatsapp: string
  customer_name: string
  email: string | null
  company: string | null
  total_orders: number
  total_spent: number
  last_order_at: string | null
  primary_service: string | null
}

export interface CustomerDetail extends CustomerSummary {
  orders: Order[]
  consultations: any[]
  payments: any[]
}

export function encodeCustomerId(whatsapp: string): string | null {
  if (!whatsapp) return null
  return Buffer.from(whatsapp, 'utf8').toString('base64url')
}

export function decodeCustomerId(id: string): string | null {
  if (!id) return null
  try {
    return Buffer.from(id, 'base64url').toString('utf8')
  } catch {
    return null
  }
}

export interface ListCustomersParams {
  search?: string
  service?: string
  sort?: 'recent' | 'spend' | 'orders'
  page?: number
  pageSize?: number
}

export interface ListCustomersResult {
  customers: CustomerSummary[]
  total: number
  page: number
  pageSize: number
  totalPages: number
}

export async function listCustomers(params: ListCustomersParams = {}): Promise<ListCustomersResult> {
  const { search, service, sort = 'recent', page = 1, pageSize = 20 } = params
  const supabase = getSupabase(true)
  if (!supabase) {
    return { customers: [], total: 0, page, pageSize, totalPages: 0 }
  }

  let query = supabase.from('orders').select('*')
  if (search) {
    query = query.or(`customer_name.ilike.%${search}%,email.ilike.%${search}%,whatsapp.ilike.%${search}%`)
  }
  if (service) {
    query = query.eq('service_type', service)
  }
  const { data: rows, error } = await query
  if (error || !rows) {
    return { customers: [], total: 0, page, pageSize, totalPages: 0 }
  }

  const map = new Map<string, CustomerSummary>()
  for (const row of rows) {
    const key = row.whatsapp
    const existing = map.get(key)
    const grandTotal = Number((row.pricing as any)?.grand_total ?? 0)
    if (!existing) {
      map.set(key, {
        id: encodeCustomerId(key)!,
        whatsapp: key,
        customer_name: row.customer_name,
        email: row.email,
        company: row.company,
        total_orders: 1,
        total_spent: grandTotal,
        last_order_at: row.created_at,
        primary_service: row.service_type
      })
    } else {
      existing.total_orders += 1
      existing.total_spent += grandTotal
      if (!existing.last_order_at || row.created_at > existing.last_order_at) {
        existing.last_order_at = row.created_at
      }
    }
  }

  let customers = Array.from(map.values())
  if (sort === 'spend') customers.sort((a, b) => b.total_spent - a.total_spent)
  else if (sort === 'orders') customers.sort((a, b) => b.total_orders - a.total_orders)
  else customers.sort((a, b) => (b.last_order_at ?? '').localeCompare(a.last_order_at ?? ''))

  const total = customers.length
  const totalPages = Math.max(1, Math.ceil(total / pageSize))
  const start = (page - 1) * pageSize
  const paged = customers.slice(start, start + pageSize)

  return { customers: paged, total, page, pageSize, totalPages }
}

export async function getCustomerDetail(id: string): Promise<CustomerDetail | null> {
  const whatsapp = decodeCustomerId(id)
  if (!whatsapp) return null
  const supabase = getSupabase(true)
  if (!supabase) return null

  const { data: orders } = await supabase
    .from('orders')
    .select('*')
    .eq('whatsapp', whatsapp)
    .order('created_at', { ascending: false })

  if (!orders || orders.length === 0) return null

  const orderIds = orders.map((o) => o.id)
  const [{ data: payments }, { data: consultations }] = await Promise.all([
    supabase.from('payments').select('*').in('order_id', orderIds),
    supabase.from('consultations').select('*').in('order_id', orderIds)
  ])

  const totalSpent = orders.reduce((sum, o) => sum + Number((o.pricing as any)?.grand_total ?? 0), 0)
  const latest = orders[0]

  return {
    id,
    whatsapp,
    customer_name: latest.customer_name,
    email: latest.email,
    company: latest.company,
    total_orders: orders.length,
    total_spent: totalSpent,
    last_order_at: latest.created_at,
    primary_service: latest.service_type,
    orders,
    payments: payments ?? [],
    consultations: consultations ?? []
  }
}
```

- [ ] **Step 4: Run test, expect PASS**

```bash
bun run test src/__tests__/admin-customers.test.ts
```

- [ ] **Step 5: Typecheck and commit**

```bash
bun run astro check
git add src/lib/admin/customers.ts src/__tests__/admin-customers.test.ts
git commit -m "feat(admin): add customer derivation from orders table"
```

---

### Task 1.6: Customers list API and page

**Files:**
- Create: `src/pages/api/admin/customers.ts`
- Create: `src/pages/admin/customers/index.astro`

- [ ] **Step 1: Create API route**

Write `src/pages/api/admin/customers.ts`:

```ts
import type { APIRoute } from 'astro'
import { listCustomers } from '../../../lib/admin/customers'
import { requireAdmin } from '../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard

  const url = new URL(ctx.request.url)
  const result = await listCustomers({
    search: url.searchParams.get('search') ?? undefined,
    service: url.searchParams.get('service') ?? undefined,
    sort: (url.searchParams.get('sort') as any) ?? 'recent',
    page: Number(url.searchParams.get('page') ?? '1') || 1
  })

  return new Response(JSON.stringify(result), {
    status: 200,
    headers: { 'Content-Type': 'application/json' }
  })
}
```

- [ ] **Step 2: Create page**

Write `src/pages/admin/customers/index.astro`:

```astro
---
import AdminLayout from '../layout.astro'
import SearchFilter from '../../../components/admin/SearchFilter.astro'
import Pagination from '../../../components/admin/Pagination.astro'
import EmptyState from '../../../components/admin/EmptyState.astro'
import { listCustomers } from '../../../lib/admin/customers'
import { Users, Eye, MessageSquare } from 'lucide-astro'

const url = new URL(Astro.request.url)
const search = url.searchParams.get('search') ?? ''
const service = url.searchParams.get('service') ?? ''
const sort = (url.searchParams.get('sort') ?? 'recent') as 'recent' | 'spend' | 'orders'
const page = Math.max(1, Number(url.searchParams.get('page') ?? '1'))

const { customers, total, totalPages } = await listCustomers({ search, service, sort, page })

const formatIDR = (n: number) =>
  new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n)

const baseUrl = `/admin/customers?search=${encodeURIComponent(search)}&service=${service}&sort=${sort}`
---
<AdminLayout title="Customers">
  <SearchFilter
    placeholder="Cari nama, email, WhatsApp..."
    value={search}
    name="search"
    action="/admin/customers"
    filters={[
      {
        name: 'service',
        label: 'Layanan',
        selected: service,
        options: [
          { value: '', label: 'Semua Layanan' },
          { value: 'website', label: 'Website' },
          { value: 'wordpress', label: 'WordPress' },
          { value: 'mobile', label: 'Mobile App' },
          { value: 'editing', label: 'Editing' },
          { value: 'service_device', label: 'Service Device' }
        ]
      },
      {
        name: 'sort',
        label: 'Urutkan',
        selected: sort,
        options: [
          { value: 'recent', label: 'Terbaru' },
          { value: 'spend', label: 'Total Spend' },
          { value: 'orders', label: 'Total Order' }
        ]
      }
    ]}
  />

  <div class="glass-card rounded-2xl p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold">{total} pelanggan</h2>
    </div>

    {customers.length === 0 ? (
      <EmptyState
        icon={Users}
        title="Belum ada pelanggan"
        description="Pelanggan akan muncul di sini setelah ada order pertama masuk."
      />
    ) : (
      <div class="overflow-x-auto">
        <table class="w-full text-sm">
          <thead>
            <tr class="border-b border-border text-left text-xs font-medium text-muted-foreground">
              <th class="pb-3">Nama</th>
              <th class="pb-3">Kontak</th>
              <th class="pb-3">Layanan</th>
              <th class="pb-3 text-right">Total Order</th>
              <th class="pb-3 text-right">Total Spend</th>
              <th class="pb-3"></th>
            </tr>
          </thead>
          <tbody>
            {customers.map((c) => (
              <tr class="border-b border-border/50 hover:bg-accent/40 transition">
                <td class="py-3">
                  <div class="font-medium">{c.customer_name}</div>
                  {c.company && <div class="text-xs text-muted-foreground">{c.company}</div>}
                </td>
                <td class="py-3 text-xs text-muted-foreground">
                  {c.email && <div>{c.email}</div>}
                  <div>{c.whatsapp}</div>
                </td>
                <td class="py-3">
                  <span class="rounded-full bg-primary/10 text-primary px-2 py-0.5 text-xs">{c.primary_service}</span>
                </td>
                <td class="py-3 text-right font-medium">{c.total_orders}</td>
                <td class="py-3 text-right font-medium text-green-600">{formatIDR(c.total_spent)}</td>
                <td class="py-3 text-right">
                  <a href={`/admin/customers/${c.id}`} class="inline-flex items-center gap-1 text-primary hover:underline text-xs">
                    <Eye class="h-3.5 w-3.5" /> Detail
                  </a>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
        <Pagination page={page} totalPages={totalPages} baseUrl={baseUrl} />
      </div>
    )}
  </div>
</AdminLayout>
```

- [ ] **Step 3: Add Customers menu item**

Edit `src/pages/admin/layout.astro`. Find the `sidebarLinks` array and add `customers` after `dashboard`:

```ts
import { Menu, X, LayoutDashboard, Users, ShoppingCart, CreditCard as Payment, MessageSquare, Settings, LogOut, User } from 'lucide-astro'

// ...
const sidebarLinks = [
  { key: 'dashboard', href: '/admin', icon: LayoutDashboard, label: 'Dashboard' },
  { key: 'customers', href: '/admin/customers', icon: Users, label: 'Customers' },
  { key: 'orders', href: '/admin/orders', icon: ShoppingCart, label: 'Orders' },
  { key: 'payments', href: '/admin/payments', icon: Payment, label: 'Payments' },
  { key: 'consultations', href: '/admin/consultations', icon: MessageSquare, label: 'Consultations' },
  { key: 'settings', href: '/admin/settings', icon: Settings, label: 'Settings' }
]
```

- [ ] **Step 4: Smoke test**

```bash
bun run dev
```

Visit `/admin/customers` (after login). Verify list renders, empty state appears if no orders, search/filter form submits with query params.

- [ ] **Step 5: Commit**

```bash
git add src/pages/api/admin/customers.ts src/pages/admin/customers/index.astro src/pages/admin/layout.astro
git commit -m "feat(admin): add customers list page derived from orders"
```

---

### Task 1.7: Customer 360° detail page

**Files:**
- Create: `src/pages/api/admin/customers/[id].ts`
- Create: `src/pages/admin/customers/[id].astro`

- [ ] **Step 1: Create detail API**

Write `src/pages/api/admin/customers/[id].ts`:

```ts
import type { APIRoute } from 'astro'
import { getCustomerDetail } from '../../../../lib/admin/customers'
import { requireAdmin } from '../../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard

  const id = ctx.params.id as string
  const detail = await getCustomerDetail(id)
  if (!detail) {
    return new Response(JSON.stringify({ error: 'Not found' }), { status: 404, headers: { 'Content-Type': 'application/json' } })
  }
  return new Response(JSON.stringify(detail), { status: 200, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 2: Create detail page**

Write `src/pages/admin/customers/[id].astro`:

```astro
---
import AdminLayout from '../layout.astro'
import { getCustomerDetail } from '../../../lib/admin/customers'
import { Mail, Phone, Building2, Calendar } from 'lucide-astro'

const id = Astro.params.id as string
const customer = await getCustomerDetail(id)
if (!customer) {
  return Astro.redirect('/admin/customers')
}

const formatIDR = (n: number) =>
  new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n)
const formatDate = (s: string | null) => s ? new Date(s).toLocaleDateString('id-ID', { day: '2-digit', month: 'short', year: 'numeric' }) : '-'
const initials = customer.customer_name.split(' ').map(w => w[0]).slice(0, 2).join('').toUpperCase()
---
<AdminLayout title={customer.customer_name}>
  <div class="grid grid-cols-1 lg:grid-cols-[280px_1fr] gap-6">
    <!-- Profile -->
    <div class="space-y-4">
      <div class="glass-card rounded-2xl p-6 text-center">
        <div class="mx-auto mb-3 h-16 w-16 rounded-full bg-primary text-primary-foreground flex items-center justify-center text-xl font-bold">
          {initials}
        </div>
        <h2 class="font-bold text-lg">{customer.customer_name}</h2>
        {customer.company && <p class="text-sm text-muted-foreground">{customer.company}</p>}
        <div class="mt-4 space-y-2 text-left text-sm">
          {customer.email && (
            <div class="flex items-center gap-2 text-muted-foreground"><Mail class="h-4 w-4" /> {customer.email}</div>
          )}
          <div class="flex items-center gap-2 text-muted-foreground"><Phone class="h-4 w-4" /> {customer.whatsapp}</div>
          {customer.company && (
            <div class="flex items-center gap-2 text-muted-foreground"><Building2 class="h-4 w-4" /> {customer.company}</div>
          )}
          <div class="flex items-center gap-2 text-muted-foreground"><Calendar class="h-4 w-4" /> Sejak {formatDate(customer.last_order_at)}</div>
        </div>
      </div>

      <div class="glass-card rounded-2xl p-6">
        <h3 class="text-xs font-bold uppercase tracking-wider text-muted-foreground mb-3">Ringkasan</h3>
        <dl class="space-y-2 text-sm">
          <div class="flex justify-between"><dt class="text-muted-foreground">Total Order</dt><dd class="font-bold">{customer.total_orders}</dd></div>
          <div class="flex justify-between"><dt class="text-muted-foreground">Total Spend</dt><dd class="font-bold text-green-600">{formatIDR(customer.total_spent)}</dd></div>
          <div class="flex justify-between"><dt class="text-muted-foreground">Konsultasi</dt><dd class="font-bold">{customer.consultations.length}</dd></div>
          <div class="flex justify-between"><dt class="text-muted-foreground">Layanan Utama</dt><dd class="font-medium">{customer.primary_service}</dd></div>
        </dl>
      </div>
    </div>

    <!-- Tabs -->
    <div class="glass-card rounded-2xl p-6">
      <div class="flex gap-2 border-b border-border mb-4 overflow-x-auto" role="tablist">
        <button class="tab-btn active px-3 py-2 text-sm font-medium" data-tab="orders">Orders ({customer.orders.length})</button>
        <button class="tab-btn px-3 py-2 text-sm font-medium text-muted-foreground" data-tab="payments">Payments ({customer.payments.length})</button>
        <button class="tab-btn px-3 py-2 text-sm font-medium text-muted-foreground" data-tab="consultations">Consultations ({customer.consultations.length})</button>
      </div>

      <div data-panel="orders">
        {customer.orders.map((o) => (
          <div class="flex items-center justify-between py-3 border-b border-border/40 text-sm">
            <div>
              <div class="font-mono text-xs text-muted-foreground">ORD-{o.id.slice(0, 8)}</div>
              <div class="font-medium">{o.service_type}</div>
              <div class="text-xs text-muted-foreground">{formatDate(o.created_at)}</div>
            </div>
            <div class="text-right">
              <div class="font-bold">{formatIDR(Number((o.pricing as any)?.grand_total ?? 0))}</div>
              <div class="text-xs text-muted-foreground">{o.status}</div>
            </div>
          </div>
        ))}
      </div>

      <div data-panel="payments" hidden>
        {customer.payments.map((p) => (
          <div class="flex items-center justify-between py-3 border-b border-border/40 text-sm">
            <div>
              <div class="font-medium">{p.gateway}</div>
              <div class="text-xs text-muted-foreground">{formatDate(p.created_at)}</div>
            </div>
            <div class="text-right">
              <div class="font-bold">{formatIDR(Number(p.amount))}</div>
              <div class="text-xs">{p.status}</div>
            </div>
          </div>
        ))}
      </div>

      <div data-panel="consultations" hidden>
        {customer.consultations.map((c) => (
          <div class="py-3 border-b border-border/40 text-sm">
            <div class="font-medium">{c.visitor_name ?? 'Konsultasi'}</div>
            <div class="text-xs text-muted-foreground">{formatDate(c.created_at)} · status {c.status}</div>
          </div>
        ))}
      </div>
    </div>
  </div>

  <style>
    .tab-btn { border-bottom: 2px solid transparent; }
    .tab-btn.active { border-bottom-color: hsl(var(--primary)); color: hsl(var(--primary)); }
  </style>

  <script>
    document.querySelectorAll<HTMLElement>('.tab-btn').forEach((btn) => {
      btn.addEventListener('click', () => {
        const tab = btn.dataset.tab!
        document.querySelectorAll<HTMLElement>('.tab-btn').forEach((b) => {
          b.classList.toggle('active', b === btn)
          b.classList.toggle('text-muted-foreground', b !== btn)
        })
        document.querySelectorAll<HTMLElement>('[data-panel]').forEach((p) => {
          p.hidden = p.dataset.panel !== tab
        })
      })
    })
  </script>
</AdminLayout>
```

- [ ] **Step 3: Smoke test** — visit `/admin/customers/<id>` for an order's whatsapp; verify orders/payments/consultations tabs switch.

- [ ] **Step 4: Commit**

```bash
git add src/pages/api/admin/customers/[id].ts src/pages/admin/customers/[id].astro
git commit -m "feat(admin): add customer 360 detail page with tabbed history"
```

---

### Task 1.8: Dashboard with real data + ApexCharts

**Files:**
- Modify: `src/pages/api/admin/dashboard.ts` — extend payload
- Create: `src/components/admin/RevenueChart.astro`
- Modify: `src/pages/admin/index.astro` — replace mock data

- [ ] **Step 1: Extend dashboard API to include 7-day revenue series and recent orders**

Overwrite `src/pages/api/admin/dashboard.ts`:

```ts
import type { APIRoute } from 'astro'
import { getSupabase } from '../../../lib/supabase'
import { requireAdmin } from '../../../lib/admin/auth'

const formatCurrency = (amount: number) =>
  new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(amount)

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard

  const supabase = getSupabase(true)
  if (!supabase) {
    return new Response(JSON.stringify({ error: 'Server not configured' }), { status: 500 })
  }

  const now = new Date()
  const todayStart = new Date(now); todayStart.setHours(0, 0, 0, 0)
  const monthStart = new Date(now.getFullYear(), now.getMonth(), 1)
  const sevenDaysAgo = new Date(now); sevenDaysAgo.setDate(sevenDaysAgo.getDate() - 6); sevenDaysAgo.setHours(0,0,0,0)

  const [paidToday, monthlyOrders, pendingPayments, paidPayments, recentOrders, weeklyPayments] = await Promise.all([
    supabase.from('payments').select('amount').eq('status', 'paid').gte('paid_at', todayStart.toISOString()),
    supabase.from('orders').select('id', { count: 'exact', head: true }).gte('created_at', monthStart.toISOString()),
    supabase.from('payments').select('id', { count: 'exact', head: true }).in('status', ['pending', 'processing']),
    supabase.from('payments').select('id', { count: 'exact', head: true }).eq('status', 'paid').gte('created_at', monthStart.toISOString()),
    supabase.from('orders').select('id, customer_name, service_type, pricing, status, created_at').order('created_at', { ascending: false }).limit(5),
    supabase.from('payments').select('amount, paid_at').eq('status', 'paid').gte('paid_at', sevenDaysAgo.toISOString())
  ])

  const todayRevenue = (paidToday.data ?? []).reduce((s, p) => s + Number(p.amount ?? 0), 0)
  const orderCount = monthlyOrders.count ?? 0
  const pending = pendingPayments.count ?? 0
  const paidCount = paidPayments.count ?? 0
  const conversionRate = orderCount > 0 ? `${((paidCount / orderCount) * 100).toFixed(1)}%` : '0%'

  // Build 7-day series
  const buckets = new Map<string, number>()
  for (let i = 0; i < 7; i++) {
    const d = new Date(sevenDaysAgo); d.setDate(d.getDate() + i)
    buckets.set(d.toISOString().slice(0, 10), 0)
  }
  for (const p of weeklyPayments.data ?? []) {
    const day = String(p.paid_at).slice(0, 10)
    buckets.set(day, (buckets.get(day) ?? 0) + Number(p.amount ?? 0))
  }

  const series = Array.from(buckets.entries()).map(([day, value]) => ({ day, value }))

  // Top services aggregated this month
  const { data: monthOrders } = await supabase
    .from('orders')
    .select('service_type, pricing')
    .gte('created_at', monthStart.toISOString())

  const svcMap = new Map<string, { orders: number; revenue: number }>()
  for (const o of monthOrders ?? []) {
    const cur = svcMap.get(o.service_type) ?? { orders: 0, revenue: 0 }
    cur.orders += 1
    cur.revenue += Number((o.pricing as any)?.grand_total ?? 0)
    svcMap.set(o.service_type, cur)
  }
  const topServices = Array.from(svcMap.entries())
    .map(([name, v]) => ({ name, ...v }))
    .sort((a, b) => b.revenue - a.revenue)
    .slice(0, 5)

  return new Response(JSON.stringify({
    stats: {
      todayRevenue: formatCurrency(todayRevenue),
      monthlyOrders: String(orderCount),
      pendingPayments: String(pending),
      conversionRate
    },
    series,
    recentOrders: recentOrders.data ?? [],
    topServices
  }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 2: Create RevenueChart component**

Write `src/components/admin/RevenueChart.astro`:

```astro
---
interface Props {
  series: Array<{ day: string; value: number }>
  height?: number
}
const { series, height = 240 } = Astro.props
const id = `revenue-chart-${Math.random().toString(36).slice(2)}`
---
<div id={id} style={`min-height:${height}px`}></div>
<script type="module" define:vars={{ id, series, height }}>
  const ApexCharts = (await import('https://cdn.jsdelivr.net/npm/apexcharts@3.49.1/+esm')).default
  const labels = series.map((s) => new Date(s.day).toLocaleDateString('id-ID', { weekday: 'short' }))
  const values = series.map((s) => s.value)
  const chart = new ApexCharts(document.getElementById(id), {
    chart: { type: 'bar', height, toolbar: { show: false }, fontFamily: 'inherit' },
    colors: ['#6366f1'],
    plotOptions: { bar: { borderRadius: 6, columnWidth: '50%' } },
    dataLabels: { enabled: false },
    series: [{ name: 'Revenue', data: values }],
    xaxis: { categories: labels, labels: { style: { colors: '#94a3b8' } }, axisBorder: { show: false }, axisTicks: { show: false } },
    yaxis: { labels: { formatter: (v) => `Rp ${Math.round(v / 1000)}k`, style: { colors: '#94a3b8' } } },
    grid: { borderColor: '#e2e8f0', strokeDashArray: 4 },
    tooltip: { y: { formatter: (v) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(v) } }
  })
  chart.render()
</script>
```

- [ ] **Step 3: Replace dashboard page with real data**

Overwrite `src/pages/admin/index.astro`:

```astro
---
import AdminLayout from './layout.astro'
import StatCard from '../../components/admin/StatCard.astro'
import RevenueChart from '../../components/admin/RevenueChart.astro'
import EmptyState from '../../components/admin/EmptyState.astro'
import { DollarSign, Package, Activity, TrendingUp, ArrowUpRight, ShoppingCart } from 'lucide-astro'
import { getSupabase } from '../../lib/supabase'

const supabase = getSupabase(true)

const now = new Date()
const todayStart = new Date(now); todayStart.setHours(0, 0, 0, 0)
const monthStart = new Date(now.getFullYear(), now.getMonth(), 1)
const sevenDaysAgo = new Date(now); sevenDaysAgo.setDate(sevenDaysAgo.getDate() - 6); sevenDaysAgo.setHours(0, 0, 0, 0)

let stats = { todayRevenue: 'Rp 0', monthlyOrders: '0', pendingPayments: '0', conversionRate: '0%' }
let series: Array<{ day: string; value: number }> = []
let recentOrders: any[] = []
let topServices: Array<{ name: string; orders: number; revenue: number }> = []

if (supabase) {
  const [paidToday, monthlyOrdersRes, pendingRes, paidRes, recentRes, weekly, monthOrdersRes] = await Promise.all([
    supabase.from('payments').select('amount').eq('status', 'paid').gte('paid_at', todayStart.toISOString()),
    supabase.from('orders').select('id', { count: 'exact', head: true }).gte('created_at', monthStart.toISOString()),
    supabase.from('payments').select('id', { count: 'exact', head: true }).in('status', ['pending', 'processing']),
    supabase.from('payments').select('id', { count: 'exact', head: true }).eq('status', 'paid').gte('created_at', monthStart.toISOString()),
    supabase.from('orders').select('id, customer_name, service_type, pricing, status, created_at').order('created_at', { ascending: false }).limit(5),
    supabase.from('payments').select('amount, paid_at').eq('status', 'paid').gte('paid_at', sevenDaysAgo.toISOString()),
    supabase.from('orders').select('service_type, pricing').gte('created_at', monthStart.toISOString())
  ])

  const todayRev = (paidToday.data ?? []).reduce((s, p) => s + Number(p.amount ?? 0), 0)
  const orderCount = monthlyOrdersRes.count ?? 0
  const pending = pendingRes.count ?? 0
  const paid = paidRes.count ?? 0
  const fmt = (n: number) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n)
  stats = {
    todayRevenue: fmt(todayRev),
    monthlyOrders: String(orderCount),
    pendingPayments: String(pending),
    conversionRate: orderCount > 0 ? `${((paid / orderCount) * 100).toFixed(1)}%` : '0%'
  }

  const buckets = new Map<string, number>()
  for (let i = 0; i < 7; i++) {
    const d = new Date(sevenDaysAgo); d.setDate(d.getDate() + i)
    buckets.set(d.toISOString().slice(0, 10), 0)
  }
  for (const p of weekly.data ?? []) {
    const day = String(p.paid_at).slice(0, 10)
    buckets.set(day, (buckets.get(day) ?? 0) + Number(p.amount ?? 0))
  }
  series = Array.from(buckets.entries()).map(([day, value]) => ({ day, value }))
  recentOrders = recentRes.data ?? []

  const svc = new Map<string, { orders: number; revenue: number }>()
  for (const o of monthOrdersRes.data ?? []) {
    const cur = svc.get(o.service_type) ?? { orders: 0, revenue: 0 }
    cur.orders += 1
    cur.revenue += Number((o.pricing as any)?.grand_total ?? 0)
    svc.set(o.service_type, cur)
  }
  topServices = Array.from(svc.entries()).map(([name, v]) => ({ name, ...v })).sort((a, b) => b.revenue - a.revenue).slice(0, 5)
}

const fmt = (n: number) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n)
---
<AdminLayout title="Dashboard">
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
    <StatCard icon={DollarSign} value={stats.todayRevenue} label="Revenue Hari Ini" />
    <StatCard icon={Package} iconColorClass="text-blue-500" value={stats.monthlyOrders} label="Order Bulan Ini" />
    <StatCard icon={Activity} iconColorClass="text-orange-500" value={stats.pendingPayments} label="Payment Pending" />
    <StatCard icon={TrendingUp} iconColorClass="text-purple-500" value={stats.conversionRate} label="Conversion Rate" />
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-6">
    <div class="lg:col-span-2 glass-card rounded-2xl p-6">
      <h2 class="text-lg font-semibold mb-4">Revenue 7 Hari Terakhir</h2>
      <RevenueChart series={series} />
    </div>
    <div class="glass-card rounded-2xl p-6">
      <h2 class="text-lg font-semibold mb-4">Top Services Bulan Ini</h2>
      {topServices.length === 0 ? (
        <p class="text-sm text-muted-foreground">Belum ada order bulan ini.</p>
      ) : (
        <div class="space-y-3">
          {topServices.map((s, i) => (
            <div class="flex items-center justify-between p-2 rounded-lg hover:bg-accent/40">
              <div class="flex items-center gap-3">
                <div class="h-7 w-7 rounded-md bg-primary/10 text-primary text-sm font-bold flex items-center justify-center">{i + 1}</div>
                <div>
                  <p class="text-sm font-medium">{s.name}</p>
                  <p class="text-xs text-muted-foreground">{s.orders} order</p>
                </div>
              </div>
              <span class="text-sm font-semibold">{fmt(s.revenue)}</span>
            </div>
          ))}
        </div>
      )}
    </div>
  </div>

  <div class="glass-card rounded-2xl p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold">Recent Orders</h2>
      <a href="/admin/orders" class="flex items-center gap-1 text-sm text-primary hover:underline">Lihat semua <ArrowUpRight class="h-4 w-4" /></a>
    </div>
    {recentOrders.length === 0 ? (
      <EmptyState icon={ShoppingCart} title="Belum ada order" description="Order baru akan muncul di sini secara real-time." />
    ) : (
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-border text-left text-xs font-medium text-muted-foreground">
            <th class="pb-2">Order ID</th><th class="pb-2">Customer</th><th class="pb-2">Layanan</th><th class="pb-2 text-right">Total</th><th class="pb-2">Status</th>
          </tr>
        </thead>
        <tbody>
          {recentOrders.map((o) => (
            <tr class="border-b border-border/40">
              <td class="py-2 font-mono text-xs">ORD-{o.id.slice(0, 8)}</td>
              <td class="py-2">{o.customer_name}</td>
              <td class="py-2">{o.service_type}</td>
              <td class="py-2 text-right font-medium">{fmt(Number((o.pricing as any)?.grand_total ?? 0))}</td>
              <td class="py-2 text-xs">{o.status}</td>
            </tr>
          ))}
        </tbody>
      </table>
    )}
  </div>
</AdminLayout>
```

- [ ] **Step 4: Smoke test** — `bun run dev` → `/admin` → verify stats are real numbers (or zeros), chart renders, no console errors.

- [ ] **Step 5: Commit**

```bash
git add src/pages/api/admin/dashboard.ts src/components/admin/RevenueChart.astro src/pages/admin/index.astro
git commit -m "feat(admin): real-data dashboard with ApexCharts revenue chart"
```

---

### Task 1.9: Real data on Orders list page

**Files:**
- Modify: `src/pages/admin/orders/index.astro` — replace mock array with Supabase query
- Modify: `src/pages/api/admin/orders.ts` — add pagination + filters

- [ ] **Step 1: Update orders API**

Overwrite `src/pages/api/admin/orders.ts`:

```ts
import type { APIRoute } from 'astro'
import { getSupabase } from '../../../lib/supabase'
import { requireAdmin } from '../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard

  const supabase = getSupabase(true)
  if (!supabase) return new Response(JSON.stringify({ orders: [], total: 0 }), { status: 200 })

  const url = new URL(ctx.request.url)
  const search = url.searchParams.get('search') ?? ''
  const service = url.searchParams.get('service') ?? ''
  const status = url.searchParams.get('status') ?? ''
  const page = Math.max(1, Number(url.searchParams.get('page') ?? '1'))
  const pageSize = 20
  const from = (page - 1) * pageSize
  const to = from + pageSize - 1

  let query = supabase.from('orders').select('*', { count: 'exact' }).order('created_at', { ascending: false }).range(from, to)
  if (search) query = query.or(`customer_name.ilike.%${search}%,email.ilike.%${search}%,whatsapp.ilike.%${search}%`)
  if (service) query = query.eq('service_type', service as any)
  if (status) query = query.eq('status', status as any)

  const { data, count } = await query
  return new Response(JSON.stringify({
    orders: data ?? [],
    total: count ?? 0,
    page,
    pageSize,
    totalPages: Math.max(1, Math.ceil((count ?? 0) / pageSize))
  }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 2: Replace orders index page**

Overwrite `src/pages/admin/orders/index.astro`:

```astro
---
import AdminLayout from '../layout.astro'
import SearchFilter from '../../../components/admin/SearchFilter.astro'
import Pagination from '../../../components/admin/Pagination.astro'
import EmptyState from '../../../components/admin/EmptyState.astro'
import { ShoppingCart, Eye } from 'lucide-astro'
import { getSupabase } from '../../../lib/supabase'

const url = new URL(Astro.request.url)
const search = url.searchParams.get('search') ?? ''
const service = url.searchParams.get('service') ?? ''
const status = url.searchParams.get('status') ?? ''
const page = Math.max(1, Number(url.searchParams.get('page') ?? '1'))
const pageSize = 20
const from = (page - 1) * pageSize
const to = from + pageSize - 1

const supabase = getSupabase(true)
let orders: any[] = []
let total = 0
if (supabase) {
  let q = supabase.from('orders').select('*', { count: 'exact' }).order('created_at', { ascending: false }).range(from, to)
  if (search) q = q.or(`customer_name.ilike.%${search}%,email.ilike.%${search}%,whatsapp.ilike.%${search}%`)
  if (service) q = q.eq('service_type', service as any)
  if (status) q = q.eq('status', status as any)
  const { data, count } = await q
  orders = data ?? []
  total = count ?? 0
}
const totalPages = Math.max(1, Math.ceil(total / pageSize))
const fmt = (n: number) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n)
const baseUrl = `/admin/orders?search=${encodeURIComponent(search)}&service=${service}&status=${status}`
---
<AdminLayout title="Orders">
  <SearchFilter
    placeholder="Cari nama, email, WA..."
    value={search}
    name="search"
    action="/admin/orders"
    filters={[
      { name: 'service', label: 'Layanan', selected: service, options: [
        { value: '', label: 'Semua Layanan' },
        { value: 'website', label: 'Website' },
        { value: 'wordpress', label: 'WordPress' },
        { value: 'mobile', label: 'Mobile App' },
        { value: 'editing', label: 'Editing' },
        { value: 'service_device', label: 'Service Device' }
      ]},
      { name: 'status', label: 'Status', selected: status, options: [
        { value: '', label: 'Semua Status' },
        { value: 'waiting_dp', label: 'Waiting DP' },
        { value: 'dp_paid', label: 'DP Paid' },
        { value: 'waiting_onsite_payment', label: 'Waiting Onsite' },
        { value: 'onsite_paid', label: 'Onsite Paid' },
        { value: 'cancelled', label: 'Cancelled' }
      ]}
    ]}
  />

  <div class="glass-card rounded-2xl p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold">{total} order</h2>
    </div>
    {orders.length === 0 ? (
      <EmptyState icon={ShoppingCart} title="Belum ada order" description="Order yang masuk akan muncul di sini." />
    ) : (
      <div class="overflow-x-auto">
        <table class="w-full text-sm">
          <thead>
            <tr class="border-b border-border text-left text-xs font-medium text-muted-foreground">
              <th class="pb-3">Order ID</th><th class="pb-3">Customer</th><th class="pb-3">Layanan</th><th class="pb-3 text-right">Total</th><th class="pb-3">Status</th><th class="pb-3"></th>
            </tr>
          </thead>
          <tbody>
            {orders.map((o) => (
              <tr class="border-b border-border/40 hover:bg-accent/40">
                <td class="py-3 font-mono text-xs">ORD-{o.id.slice(0, 8)}</td>
                <td class="py-3">
                  <div class="font-medium">{o.customer_name}</div>
                  <div class="text-xs text-muted-foreground">{o.whatsapp}</div>
                </td>
                <td class="py-3"><span class="rounded-full bg-primary/10 text-primary px-2 py-0.5 text-xs">{o.service_type}</span></td>
                <td class="py-3 text-right font-medium">{fmt(Number((o.pricing as any)?.grand_total ?? 0))}</td>
                <td class="py-3 text-xs">{o.status}</td>
                <td class="py-3 text-right">
                  <a href={`/admin/orders/${o.id}`} class="inline-flex items-center gap-1 text-primary hover:underline text-xs">
                    <Eye class="h-3.5 w-3.5" /> Detail
                  </a>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
        <Pagination page={page} totalPages={totalPages} baseUrl={baseUrl} />
      </div>
    )}
  </div>
</AdminLayout>
```

- [ ] **Step 3: Smoke test** — visit `/admin/orders`, verify list shows real data or empty state.

- [ ] **Step 4: Commit**

```bash
git add src/pages/admin/orders/index.astro src/pages/api/admin/orders.ts
git commit -m "feat(admin): real-data orders list with filters and pagination"
```

---

### Task 1.10: Real data on Payments list page

**Files:**
- Modify: `src/pages/admin/payments/index.astro`

- [ ] **Step 1: Read existing file to preserve structure**

```bash
cat src/pages/admin/payments/index.astro | head -40
```

- [ ] **Step 2: Replace mock data block** — overwrite the file with:

```astro
---
import AdminLayout from '../layout.astro'
import SearchFilter from '../../../components/admin/SearchFilter.astro'
import Pagination from '../../../components/admin/Pagination.astro'
import EmptyState from '../../../components/admin/EmptyState.astro'
import { CreditCard } from 'lucide-astro'
import { getSupabase } from '../../../lib/supabase'

const url = new URL(Astro.request.url)
const status = url.searchParams.get('status') ?? ''
const gateway = url.searchParams.get('gateway') ?? ''
const page = Math.max(1, Number(url.searchParams.get('page') ?? '1'))
const pageSize = 20
const from = (page - 1) * pageSize
const to = from + pageSize - 1

const supabase = getSupabase(true)
let payments: any[] = []
let total = 0
if (supabase) {
  let q = supabase.from('payments').select('*, orders(customer_name, whatsapp)', { count: 'exact' }).order('created_at', { ascending: false }).range(from, to)
  if (status) q = q.eq('status', status as any)
  if (gateway) q = q.eq('gateway', gateway as any)
  const { data, count } = await q
  payments = data ?? []
  total = count ?? 0
}
const totalPages = Math.max(1, Math.ceil(total / pageSize))
const fmt = (n: number) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n)
const baseUrl = `/admin/payments?status=${status}&gateway=${gateway}`
---
<AdminLayout title="Payments">
  <SearchFilter
    placeholder="Cari..."
    value=""
    name="search"
    action="/admin/payments"
    filters={[
      { name: 'status', label: 'Status', selected: status, options: [
        { value: '', label: 'Semua Status' },
        { value: 'pending', label: 'Pending' },
        { value: 'processing', label: 'Processing' },
        { value: 'paid', label: 'Paid' },
        { value: 'failed', label: 'Failed' },
        { value: 'expired', label: 'Expired' },
        { value: 'refunded', label: 'Refunded' }
      ]},
      { name: 'gateway', label: 'Gateway', selected: gateway, options: [
        { value: '', label: 'Semua Gateway' },
        { value: 'midtrans', label: 'Midtrans' },
        { value: 'pakasir', label: 'Pakasir' },
        { value: 'qiospay', label: 'Qiospay' },
        { value: 'sanpay', label: 'Sanpay' },
        { value: 'tripay', label: 'Tripay' }
      ]}
    ]}
  />

  <div class="glass-card rounded-2xl p-6">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold">{total} payment</h2>
    </div>
    {payments.length === 0 ? (
      <EmptyState icon={CreditCard} title="Belum ada payment" />
    ) : (
      <div class="overflow-x-auto">
        <table class="w-full text-sm">
          <thead>
            <tr class="border-b border-border text-left text-xs font-medium text-muted-foreground">
              <th class="pb-3">ID</th><th class="pb-3">Customer</th><th class="pb-3">Gateway</th><th class="pb-3 text-right">Amount</th><th class="pb-3">Status</th><th class="pb-3">Created</th>
            </tr>
          </thead>
          <tbody>
            {payments.map((p) => (
              <tr class="border-b border-border/40">
                <td class="py-3 font-mono text-xs">PAY-{p.id.slice(0, 8)}</td>
                <td class="py-3">
                  <div class="font-medium">{p.orders?.customer_name ?? '-'}</div>
                  <div class="text-xs text-muted-foreground">{p.orders?.whatsapp ?? ''}</div>
                </td>
                <td class="py-3"><span class="rounded-full bg-primary/10 text-primary px-2 py-0.5 text-xs">{p.gateway}</span></td>
                <td class="py-3 text-right font-medium">{fmt(Number(p.amount))}</td>
                <td class="py-3 text-xs">{p.status}</td>
                <td class="py-3 text-xs text-muted-foreground">{new Date(p.created_at).toLocaleDateString('id-ID')}</td>
              </tr>
            ))}
          </tbody>
        </table>
        <Pagination page={page} totalPages={totalPages} baseUrl={baseUrl} />
      </div>
    )}
  </div>
</AdminLayout>
```

- [ ] **Step 3: Smoke test** + commit

```bash
bun run astro check
git add src/pages/admin/payments/index.astro
git commit -m "feat(admin): real-data payments list with filters and pagination"
```

---

### Task 1.11: Phase 1 verification checklist

- [ ] **Step 1:** Run typecheck: `bun run astro check` → 0 errors
- [ ] **Step 2:** Run tests: `bun run test` → all PASS
- [ ] **Step 3:** Smoke test the full flow:
  - Visit `/admin` while logged out → redirected to `/admin/login`
  - Log in with valid credentials → land on `/admin` showing real stats
  - Click Customers → see derived list
  - Click a customer → see 360° detail with tabs
  - Click Orders → see real list with filters
  - Click Payments → see real list with filters
  - Click Logout → redirected to login

- [ ] **Step 4: Tag Phase 1 milestone**

```bash
git tag phase-1-foundation
```

---

## PHASE 2 — Pipeline & Activities

> Spec reference: §2 Phase 2, §4.2 Schema, §5.6 Pipeline, §5.7 Activities, §8 Notifications

### Task 2.1: Apply SQL migration for `leads` and `activities`

**Files:**
- Create: `scripts/crm-phase2-schema.sql`

- [ ] **Step 1:** Create migration file:

Write `scripts/crm-phase2-schema.sql`:

```sql
-- ============================================================
-- CRM Phase 2: leads + activities
-- Run in Supabase SQL Editor
-- ============================================================

-- 1. Enums
DO $$ BEGIN
  CREATE TYPE lead_stage AS ENUM ('new','contacted','qualified','proposal','negotiation','won','lost');
EXCEPTION WHEN duplicate_object THEN NULL; END $$;

DO $$ BEGIN
  CREATE TYPE activity_type AS ENUM ('note','call','meeting','follow_up','task','email');
EXCEPTION WHEN duplicate_object THEN NULL; END $$;

-- 2. leads table
CREATE TABLE IF NOT EXISTS public.leads (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  name TEXT NOT NULL,
  whatsapp TEXT,
  email TEXT,
  company TEXT,
  service_interest TEXT,
  stage lead_stage NOT NULL DEFAULT 'new',
  source TEXT DEFAULT 'manual',
  consultation_id UUID REFERENCES public.consultations(id) ON DELETE SET NULL,
  order_id UUID REFERENCES public.orders(id) ON DELETE SET NULL,
  notes TEXT,
  estimated_value BIGINT,
  closed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_leads_stage ON public.leads(stage);
CREATE INDEX IF NOT EXISTS idx_leads_whatsapp ON public.leads(whatsapp);
CREATE INDEX IF NOT EXISTS idx_leads_consultation_id ON public.leads(consultation_id);
CREATE INDEX IF NOT EXISTS idx_leads_order_id ON public.leads(order_id);

DROP TRIGGER IF EXISTS set_leads_updated_at ON public.leads;
CREATE TRIGGER set_leads_updated_at
  BEFORE UPDATE ON public.leads
  FOR EACH ROW EXECUTE FUNCTION public.handle_updated_at();

-- 3. activities table
CREATE TABLE IF NOT EXISTS public.activities (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  type activity_type NOT NULL DEFAULT 'note',
  title TEXT NOT NULL,
  description TEXT,
  lead_id UUID REFERENCES public.leads(id) ON DELETE CASCADE,
  order_id UUID REFERENCES public.orders(id) ON DELETE CASCADE,
  due_date TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  is_completed BOOLEAN NOT NULL DEFAULT false,
  priority TEXT NOT NULL DEFAULT 'medium',
  created_by TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

ALTER TABLE public.activities DROP CONSTRAINT IF EXISTS activity_target_required;
ALTER TABLE public.activities ADD CONSTRAINT activity_target_required
  CHECK (lead_id IS NOT NULL OR order_id IS NOT NULL);

CREATE INDEX IF NOT EXISTS idx_activities_lead_id ON public.activities(lead_id);
CREATE INDEX IF NOT EXISTS idx_activities_order_id ON public.activities(order_id);
CREATE INDEX IF NOT EXISTS idx_activities_due ON public.activities(due_date) WHERE is_completed = false;

-- 4. RLS (admin uses service role, but enable RLS for safety)
ALTER TABLE public.leads ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.activities ENABLE ROW LEVEL SECURITY;
```

- [ ] **Step 2:** Apply the migration in Supabase Dashboard → SQL Editor → paste contents → Run.

- [ ] **Step 3:** Verify tables exist:

In Supabase SQL editor: `SELECT count(*) FROM leads; SELECT count(*) FROM activities;` should both return 0.

- [ ] **Step 4: Commit**

```bash
git add scripts/crm-phase2-schema.sql
git commit -m "chore(db): add Phase 2 migration for leads and activities tables"
```

---

### Task 2.2: Update generated database types

**Files:**
- Modify: `src/types/database.ts`

- [ ] **Step 1:** Add `leads` and `activities` to the `Tables` interface and `Enums` block.

Open `src/types/database.ts`. Inside `Tables: {`, after the `consultation_messages` entry, add:

```ts
      leads: {
        Row: {
          id: string
          name: string
          whatsapp: string | null
          email: string | null
          company: string | null
          service_interest: string | null
          stage: Database['public']['Enums']['lead_stage']
          source: string
          consultation_id: string | null
          order_id: string | null
          notes: string | null
          estimated_value: number | null
          closed_at: string | null
          created_at: string
          updated_at: string
        }
        Insert: {
          id?: string
          name: string
          whatsapp?: string | null
          email?: string | null
          company?: string | null
          service_interest?: string | null
          stage?: Database['public']['Enums']['lead_stage']
          source?: string
          consultation_id?: string | null
          order_id?: string | null
          notes?: string | null
          estimated_value?: number | null
          closed_at?: string | null
          created_at?: string
          updated_at?: string
        }
        Update: Partial<Database['public']['Tables']['leads']['Insert']>
        Relationships: [
          { foreignKeyName: 'leads_consultation_id_fkey'; columns: ['consultation_id']; isOneToOne: false; referencedRelation: 'consultations'; referencedColumns: ['id'] },
          { foreignKeyName: 'leads_order_id_fkey'; columns: ['order_id']; isOneToOne: false; referencedRelation: 'orders'; referencedColumns: ['id'] }
        ]
      }
      activities: {
        Row: {
          id: string
          type: Database['public']['Enums']['activity_type']
          title: string
          description: string | null
          lead_id: string | null
          order_id: string | null
          due_date: string | null
          completed_at: string | null
          is_completed: boolean
          priority: string
          created_by: string | null
          created_at: string
        }
        Insert: {
          id?: string
          type?: Database['public']['Enums']['activity_type']
          title: string
          description?: string | null
          lead_id?: string | null
          order_id?: string | null
          due_date?: string | null
          completed_at?: string | null
          is_completed?: boolean
          priority?: string
          created_by?: string | null
          created_at?: string
        }
        Update: Partial<Database['public']['Tables']['activities']['Insert']>
        Relationships: [
          { foreignKeyName: 'activities_lead_id_fkey'; columns: ['lead_id']; isOneToOne: false; referencedRelation: 'leads'; referencedColumns: ['id'] },
          { foreignKeyName: 'activities_order_id_fkey'; columns: ['order_id']; isOneToOne: false; referencedRelation: 'orders'; referencedColumns: ['id'] }
        ]
      }
```

In the `Enums` block, add:

```ts
      lead_stage: 'new' | 'contacted' | 'qualified' | 'proposal' | 'negotiation' | 'won' | 'lost'
      activity_type: 'note' | 'call' | 'meeting' | 'follow_up' | 'task' | 'email'
```

At the bottom of the file (after existing `export type` aliases), add:

```ts
export type Lead = Tables<'leads'>
export type LeadInsert = TablesInsert<'leads'>
export type LeadUpdate = TablesUpdate<'leads'>
export type Activity = Tables<'activities'>
export type ActivityInsert = TablesInsert<'activities'>
export type ActivityUpdate = TablesUpdate<'activities'>
export type LeadStage = Database['public']['Enums']['lead_stage']
export type ActivityType = Database['public']['Enums']['activity_type']
```

- [ ] **Step 2:** `bun run astro check` — expect 0 errors.

- [ ] **Step 3: Commit**

```bash
git add src/types/database.ts
git commit -m "feat(types): add leads and activities to database types"
```

---

### Task 2.3: Leads helper module + tests

**Files:**
- Create: `src/lib/admin/leads.ts`
- Test: `src/__tests__/admin-leads.test.ts`

- [ ] **Step 1: Write test for stage transition validation**

```ts
import { describe, it, expect } from 'vitest'
import { isValidStageTransition, LEAD_STAGE_ORDER } from '../lib/admin/leads'

describe('lead stage transitions', () => {
  it('always allows moving to lost', () => {
    expect(isValidStageTransition('new', 'lost')).toBe(true)
    expect(isValidStageTransition('proposal', 'lost')).toBe(true)
  })

  it('allows forward progression', () => {
    expect(isValidStageTransition('new', 'contacted')).toBe(true)
    expect(isValidStageTransition('contacted', 'qualified')).toBe(true)
  })

  it('allows skipping ahead one or more stages', () => {
    expect(isValidStageTransition('new', 'proposal')).toBe(true)
  })

  it('allows backward movement (rare but valid)', () => {
    expect(isValidStageTransition('proposal', 'qualified')).toBe(true)
  })

  it('rejects revival from won/lost', () => {
    expect(isValidStageTransition('won', 'proposal')).toBe(false)
    expect(isValidStageTransition('lost', 'new')).toBe(false)
  })

  it('exposes a stable order', () => {
    expect(LEAD_STAGE_ORDER).toContain('new')
    expect(LEAD_STAGE_ORDER[0]).toBe('new')
  })
})
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement helper**

Write `src/lib/admin/leads.ts`:

```ts
import { getSupabase } from '../supabase'
import type { Lead, LeadInsert, LeadUpdate, LeadStage } from '../../types/database'

export const LEAD_STAGE_ORDER: LeadStage[] = ['new', 'contacted', 'qualified', 'proposal', 'negotiation', 'won', 'lost']

export const LEAD_STAGE_LABELS: Record<LeadStage, string> = {
  new: 'Baru',
  contacted: 'Dihubungi',
  qualified: 'Qualified',
  proposal: 'Proposal',
  negotiation: 'Negosiasi',
  won: 'Won',
  lost: 'Lost'
}

export function isValidStageTransition(from: LeadStage, to: LeadStage): boolean {
  if (to === 'lost') return from !== 'won' && from !== 'lost'
  if (from === 'won' || from === 'lost') return false
  return true
}

export async function listLeadsByStage(): Promise<Record<LeadStage, Lead[]>> {
  const supabase = getSupabase(true)
  const empty = Object.fromEntries(LEAD_STAGE_ORDER.map((s) => [s, []])) as Record<LeadStage, Lead[]>
  if (!supabase) return empty

  const { data } = await supabase.from('leads').select('*').order('updated_at', { ascending: false })
  for (const lead of data ?? []) {
    empty[lead.stage].push(lead as Lead)
  }
  return empty
}

export async function getLead(id: string): Promise<Lead | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  const { data } = await supabase.from('leads').select('*').eq('id', id).single()
  return (data as Lead) ?? null
}

export async function createLead(payload: LeadInsert): Promise<Lead | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  const { data } = await supabase.from('leads').insert(payload).select().single()
  return (data as Lead) ?? null
}

export async function updateLead(id: string, payload: LeadUpdate): Promise<Lead | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  if (payload.stage === 'won' || payload.stage === 'lost') {
    payload.closed_at = new Date().toISOString()
  }
  const { data } = await supabase.from('leads').update(payload).eq('id', id).select().single()
  return (data as Lead) ?? null
}

export async function deleteLead(id: string): Promise<boolean> {
  const supabase = getSupabase(true)
  if (!supabase) return false
  const { error } = await supabase.from('leads').delete().eq('id', id)
  return !error
}

export async function countLeadsByStage(): Promise<Record<LeadStage, number>> {
  const supabase = getSupabase(true)
  const empty = Object.fromEntries(LEAD_STAGE_ORDER.map((s) => [s, 0])) as Record<LeadStage, number>
  if (!supabase) return empty
  const { data } = await supabase.from('leads').select('stage')
  for (const row of data ?? []) empty[row.stage as LeadStage] += 1
  return empty
}

export async function totalEstimatedValueOpen(): Promise<number> {
  const supabase = getSupabase(true)
  if (!supabase) return 0
  const { data } = await supabase
    .from('leads')
    .select('estimated_value')
    .not('stage', 'in', '("won","lost")')
  return (data ?? []).reduce((sum, r) => sum + Number(r.estimated_value ?? 0), 0)
}
```

- [ ] **Step 4: Run test, expect PASS**

- [ ] **Step 5: Commit**

```bash
git add src/lib/admin/leads.ts src/__tests__/admin-leads.test.ts
git commit -m "feat(admin): leads helper with stage transitions and queries"
```

---

### Task 2.4: Leads API routes

**Files:**
- Create: `src/pages/api/admin/leads.ts`
- Create: `src/pages/api/admin/leads/[id].ts`

- [ ] **Step 1: Create list/create endpoint**

Write `src/pages/api/admin/leads.ts`:

```ts
import type { APIRoute } from 'astro'
import { createLead, listLeadsByStage } from '../../../lib/admin/leads'
import { requireAdmin } from '../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const board = await listLeadsByStage()
  return new Response(JSON.stringify({ board }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}

export const POST: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const body = await ctx.request.json()
  if (!body.name || typeof body.name !== 'string') {
    return new Response(JSON.stringify({ error: 'name is required' }), { status: 400 })
  }
  const lead = await createLead({
    name: body.name,
    whatsapp: body.whatsapp ?? null,
    email: body.email ?? null,
    company: body.company ?? null,
    service_interest: body.service_interest ?? null,
    stage: body.stage ?? 'new',
    source: body.source ?? 'manual',
    notes: body.notes ?? null,
    estimated_value: body.estimated_value ?? null
  })
  if (!lead) return new Response(JSON.stringify({ error: 'Failed to create' }), { status: 500 })
  return new Response(JSON.stringify({ lead }), { status: 201, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 2: Create [id] endpoint**

Write `src/pages/api/admin/leads/[id].ts`:

```ts
import type { APIRoute } from 'astro'
import { deleteLead, getLead, isValidStageTransition, updateLead } from '../../../../lib/admin/leads'
import { requireAdmin } from '../../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const lead = await getLead(ctx.params.id as string)
  if (!lead) return new Response(JSON.stringify({ error: 'Not found' }), { status: 404 })
  return new Response(JSON.stringify({ lead }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}

export const PATCH: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const body = await ctx.request.json()
  const id = ctx.params.id as string
  if (body.stage) {
    const current = await getLead(id)
    if (!current) return new Response(JSON.stringify({ error: 'Not found' }), { status: 404 })
    if (!isValidStageTransition(current.stage, body.stage)) {
      return new Response(JSON.stringify({ error: 'Invalid stage transition' }), { status: 400 })
    }
  }
  const updated = await updateLead(id, body)
  if (!updated) return new Response(JSON.stringify({ error: 'Update failed' }), { status: 500 })
  return new Response(JSON.stringify({ lead: updated }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}

export const DELETE: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const ok = await deleteLead(ctx.params.id as string)
  return new Response(JSON.stringify({ success: ok }), { status: ok ? 200 : 500 })
}
```

- [ ] **Step 3: Commit**

```bash
git add src/pages/api/admin/leads.ts src/pages/api/admin/leads/[id].ts
git commit -m "feat(admin): leads CRUD API"
```

---

### Task 2.5: Pipeline kanban page

**Files:**
- Create: `src/components/admin/PipelineKanban.astro`
- Create: `src/pages/admin/pipeline/index.astro`

- [ ] **Step 1: Create kanban component with drag-drop**

Write `src/components/admin/PipelineKanban.astro`:

```astro
---
import type { Lead, LeadStage } from '../../types/database'
import { LEAD_STAGE_LABELS, LEAD_STAGE_ORDER } from '../../lib/admin/leads'

interface Props {
  board: Record<LeadStage, Lead[]>
}
const { board } = Astro.props

const fmt = (n: number | null) => n ? new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n) : ''

const stageColors: Record<LeadStage, string> = {
  new: '#6b7280',
  contacted: '#3b82f6',
  qualified: '#06b6d4',
  proposal: '#f59e0b',
  negotiation: '#8b5cf6',
  won: '#22c55e',
  lost: '#ef4444'
}
---
<div class="flex gap-3 overflow-x-auto pb-2" id="kanban-board">
  {LEAD_STAGE_ORDER.map((stage) => (
    <div class="kanban-col flex-shrink-0 w-64 glass-card rounded-2xl p-3" data-stage={stage}>
      <div class="flex items-center justify-between mb-3 px-1">
        <div class="flex items-center gap-2">
          <span class="h-2 w-2 rounded-full" style={`background:${stageColors[stage]}`}></span>
          <span class="text-xs font-bold uppercase tracking-wider">{LEAD_STAGE_LABELS[stage]}</span>
          <span class="text-xs text-muted-foreground">({board[stage].length})</span>
        </div>
      </div>
      <div class="kanban-list space-y-2 min-h-[100px]" data-stage={stage}>
        {board[stage].map((lead) => (
          <div
            class="kanban-card rounded-lg bg-background border border-border p-3 cursor-grab active:cursor-grabbing"
            draggable="true"
            data-id={lead.id}
            style={`border-left: 3px solid ${stageColors[stage]}`}
          >
            <div class="font-medium text-sm">{lead.name}</div>
            {lead.service_interest && <div class="text-xs text-muted-foreground mt-0.5">{lead.service_interest}</div>}
            {lead.estimated_value && <div class="text-xs font-medium mt-1" style={`color:${stageColors[stage]}`}>{fmt(Number(lead.estimated_value))}</div>}
          </div>
        ))}
      </div>
    </div>
  ))}
</div>

<script>
  const board = document.getElementById('kanban-board')
  if (board) {
    let draggedId: string | null = null

    board.querySelectorAll<HTMLElement>('.kanban-card').forEach((card) => {
      card.addEventListener('dragstart', (e) => {
        draggedId = card.dataset.id ?? null
        card.classList.add('opacity-50')
      })
      card.addEventListener('dragend', () => {
        card.classList.remove('opacity-50')
        draggedId = null
      })
    })

    board.querySelectorAll<HTMLElement>('.kanban-list').forEach((list) => {
      list.addEventListener('dragover', (e) => {
        e.preventDefault()
        list.classList.add('bg-accent/30', 'rounded-lg')
      })
      list.addEventListener('dragleave', () => {
        list.classList.remove('bg-accent/30', 'rounded-lg')
      })
      list.addEventListener('drop', async (e) => {
        e.preventDefault()
        list.classList.remove('bg-accent/30', 'rounded-lg')
        if (!draggedId) return
        const newStage = list.dataset.stage
        const res = await fetch(`/api/admin/leads/${draggedId}`, {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ stage: newStage })
        })
        if (res.ok) {
          window.location.reload()
        } else {
          const { error } = await res.json().catch(() => ({ error: 'Gagal' }))
          alert(`Gagal pindah stage: ${error}`)
        }
      })
    })
  }
</script>
```

- [ ] **Step 2: Create pipeline page**

Write `src/pages/admin/pipeline/index.astro`:

```astro
---
import AdminLayout from '../layout.astro'
import PipelineKanban from '../../../components/admin/PipelineKanban.astro'
import { listLeadsByStage, totalEstimatedValueOpen, countLeadsByStage } from '../../../lib/admin/leads'
import { Plus } from 'lucide-astro'

const board = await listLeadsByStage()
const counts = await countLeadsByStage()
const totalOpen = await totalEstimatedValueOpen()
const fmt = (n: number) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n)
const totalLeads = Object.values(counts).reduce((s, n) => s + n, 0)
---
<AdminLayout title="Pipeline">
  <div class="flex items-center justify-between mb-4">
    <div>
      <p class="text-sm text-muted-foreground">{totalLeads} lead aktif · Estimasi nilai terbuka {fmt(totalOpen)}</p>
    </div>
    <button id="new-lead-btn" class="inline-flex items-center gap-1 rounded-xl bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90">
      <Plus class="h-4 w-4" /> Lead Baru
    </button>
  </div>

  <PipelineKanban board={board} />

  <!-- Modal: New Lead -->
  <dialog id="new-lead-dialog" class="rounded-2xl p-0 backdrop:bg-black/50">
    <form id="new-lead-form" class="bg-background p-6 w-[420px] max-w-[90vw]">
      <h2 class="text-lg font-semibold mb-4">Lead Baru</h2>
      <div class="space-y-3">
        <input name="name" required placeholder="Nama" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        <input name="whatsapp" placeholder="WhatsApp" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        <input name="email" type="email" placeholder="Email" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        <input name="company" placeholder="Perusahaan" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        <input name="service_interest" placeholder="Layanan yang diminati" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        <input name="estimated_value" type="number" placeholder="Estimasi nilai (Rp)" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        <textarea name="notes" placeholder="Catatan" rows="3" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm"></textarea>
      </div>
      <div class="mt-4 flex justify-end gap-2">
        <button type="button" id="cancel-new-lead" class="rounded-lg border border-border px-4 py-2 text-sm">Batal</button>
        <button type="submit" class="rounded-lg bg-primary px-4 py-2 text-sm text-primary-foreground">Simpan</button>
      </div>
    </form>
  </dialog>

  <script>
    const dialog = document.getElementById('new-lead-dialog') as HTMLDialogElement
    const openBtn = document.getElementById('new-lead-btn')
    const cancelBtn = document.getElementById('cancel-new-lead')
    const form = document.getElementById('new-lead-form') as HTMLFormElement
    openBtn?.addEventListener('click', () => dialog.showModal())
    cancelBtn?.addEventListener('click', () => dialog.close())
    form?.addEventListener('submit', async (e) => {
      e.preventDefault()
      const data = Object.fromEntries(new FormData(form))
      if (data.estimated_value) data.estimated_value = String(Number(data.estimated_value)) as any
      const res = await fetch('/api/admin/leads', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
      })
      if (res.ok) {
        dialog.close()
        window.location.reload()
      } else {
        alert('Gagal membuat lead')
      }
    })
  </script>
</AdminLayout>
```

- [ ] **Step 3:** Add Pipeline link to sidebar in `src/pages/admin/layout.astro`. Import `Columns3` icon and insert after `customers`:

```ts
import { Menu, X, LayoutDashboard, Users, Columns3, ShoppingCart, CreditCard as Payment, MessageSquare, Settings, LogOut, User } from 'lucide-astro'

// in sidebarLinks, after customers:
  { key: 'pipeline', href: '/admin/pipeline', icon: Columns3, label: 'Pipeline' },
```

- [ ] **Step 4: Smoke test** — visit `/admin/pipeline`, click "Lead Baru", create test lead, drag between columns.

- [ ] **Step 5: Commit**

```bash
git add src/components/admin/PipelineKanban.astro src/pages/admin/pipeline/ src/pages/admin/layout.astro
git commit -m "feat(admin): pipeline kanban with drag-drop stage transitions"
```

---

### Task 2.6: Activities helper, API, and page

**Files:**
- Create: `src/lib/admin/activities.ts`
- Create: `src/pages/api/admin/activities.ts`
- Create: `src/pages/api/admin/activities/[id].ts`
- Create: `src/pages/admin/activities/index.astro`

- [ ] **Step 1: Implement helper**

Write `src/lib/admin/activities.ts`:

```ts
import { getSupabase } from '../supabase'
import type { Activity, ActivityInsert, ActivityUpdate } from '../../types/database'

export type ActivityFilter = 'today' | 'overdue' | 'upcoming' | 'completed' | 'all'

export async function listActivities(filter: ActivityFilter = 'all'): Promise<Activity[]> {
  const supabase = getSupabase(true)
  if (!supabase) return []
  let q = supabase.from('activities').select('*').order('due_date', { ascending: true, nullsFirst: false })

  const now = new Date()
  const todayEnd = new Date(now); todayEnd.setHours(23, 59, 59, 999)

  if (filter === 'today') {
    q = q.eq('is_completed', false).gte('due_date', now.toISOString().slice(0, 10)).lte('due_date', todayEnd.toISOString())
  } else if (filter === 'overdue') {
    q = q.eq('is_completed', false).lt('due_date', now.toISOString())
  } else if (filter === 'upcoming') {
    q = q.eq('is_completed', false).gt('due_date', todayEnd.toISOString())
  } else if (filter === 'completed') {
    q = q.eq('is_completed', true)
  }
  const { data } = await q
  return (data ?? []) as Activity[]
}

export async function createActivity(payload: ActivityInsert): Promise<Activity | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  if (!payload.lead_id && !payload.order_id) return null
  const { data } = await supabase.from('activities').insert(payload).select().single()
  return (data as Activity) ?? null
}

export async function updateActivity(id: string, payload: ActivityUpdate): Promise<Activity | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  if (payload.is_completed && !payload.completed_at) {
    payload.completed_at = new Date().toISOString()
  }
  const { data } = await supabase.from('activities').update(payload).eq('id', id).select().single()
  return (data as Activity) ?? null
}

export async function deleteActivity(id: string): Promise<boolean> {
  const supabase = getSupabase(true)
  if (!supabase) return false
  const { error } = await supabase.from('activities').delete().eq('id', id)
  return !error
}

export async function countOverdueActivities(): Promise<number> {
  const supabase = getSupabase(true)
  if (!supabase) return 0
  const { count } = await supabase
    .from('activities')
    .select('id', { count: 'exact', head: true })
    .eq('is_completed', false)
    .lt('due_date', new Date().toISOString())
  return count ?? 0
}
```

- [ ] **Step 2: Create API route**

Write `src/pages/api/admin/activities.ts`:

```ts
import type { APIRoute } from 'astro'
import { createActivity, listActivities } from '../../../lib/admin/activities'
import { requireAdmin } from '../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const url = new URL(ctx.request.url)
  const filter = (url.searchParams.get('filter') as any) ?? 'all'
  const items = await listActivities(filter)
  return new Response(JSON.stringify({ items }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}

export const POST: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const body = await ctx.request.json()
  if (!body.title) return new Response(JSON.stringify({ error: 'title required' }), { status: 400 })
  if (!body.lead_id && !body.order_id) return new Response(JSON.stringify({ error: 'lead_id or order_id required' }), { status: 400 })
  const created = await createActivity({
    type: body.type ?? 'note',
    title: body.title,
    description: body.description ?? null,
    lead_id: body.lead_id ?? null,
    order_id: body.order_id ?? null,
    due_date: body.due_date ?? null,
    priority: body.priority ?? 'medium',
    created_by: guard.displayName
  })
  if (!created) return new Response(JSON.stringify({ error: 'failed' }), { status: 500 })
  return new Response(JSON.stringify({ activity: created }), { status: 201 })
}
```

Write `src/pages/api/admin/activities/[id].ts`:

```ts
import type { APIRoute } from 'astro'
import { deleteActivity, updateActivity } from '../../../../lib/admin/activities'
import { requireAdmin } from '../../../../lib/admin/auth'

export const PATCH: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const body = await ctx.request.json()
  const updated = await updateActivity(ctx.params.id as string, body)
  if (!updated) return new Response(JSON.stringify({ error: 'failed' }), { status: 500 })
  return new Response(JSON.stringify({ activity: updated }), { status: 200 })
}

export const DELETE: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const ok = await deleteActivity(ctx.params.id as string)
  return new Response(JSON.stringify({ success: ok }), { status: ok ? 200 : 500 })
}
```

- [ ] **Step 3: Create activities page**

Write `src/pages/admin/activities/index.astro`:

```astro
---
import AdminLayout from '../layout.astro'
import EmptyState from '../../../components/admin/EmptyState.astro'
import { CheckSquare } from 'lucide-astro'
import { listActivities, type ActivityFilter } from '../../../lib/admin/activities'

const url = new URL(Astro.request.url)
const filter = (url.searchParams.get('filter') ?? 'today') as ActivityFilter
const activities = await listActivities(filter)

const filters: Array<{ key: ActivityFilter; label: string }> = [
  { key: 'today', label: 'Hari ini' },
  { key: 'overdue', label: 'Terlambat' },
  { key: 'upcoming', label: 'Mendatang' },
  { key: 'completed', label: 'Selesai' },
  { key: 'all', label: 'Semua' }
]
const fmt = (s: string | null) => s ? new Date(s).toLocaleString('id-ID', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }) : '-'
---
<AdminLayout title="Activities">
  <div class="flex gap-2 mb-4 overflow-x-auto">
    {filters.map((f) => (
      <a
        href={`/admin/activities?filter=${f.key}`}
        class:list={[
          'rounded-full border px-3 py-1 text-sm whitespace-nowrap',
          filter === f.key ? 'border-primary bg-primary text-primary-foreground' : 'border-border text-muted-foreground hover:bg-accent/40'
        ]}
      >
        {f.label}
      </a>
    ))}
  </div>

  <div class="glass-card rounded-2xl p-6">
    {activities.length === 0 ? (
      <EmptyState icon={CheckSquare} title="Tidak ada aktivitas" description="Aktivitas akan muncul setelah ada follow-up atau task yang dibuat." />
    ) : (
      <ul class="space-y-2">
        {activities.map((a) => (
          <li class="flex items-start gap-3 p-3 rounded-lg border border-border/40 hover:bg-accent/30">
            <input
              type="checkbox"
              class="mt-1 toggle-activity"
              data-id={a.id}
              checked={a.is_completed}
            />
            <div class="flex-1">
              <div class="flex items-center gap-2">
                <span class="text-xs uppercase tracking-wider px-1.5 py-0.5 rounded bg-muted text-muted-foreground">{a.type}</span>
                <span class="font-medium" class:list={[a.is_completed && 'line-through text-muted-foreground']}>{a.title}</span>
              </div>
              {a.description && <p class="text-sm text-muted-foreground mt-1">{a.description}</p>}
              <p class="text-xs text-muted-foreground mt-1">Due {fmt(a.due_date)} · prio {a.priority}</p>
            </div>
            <button class="text-xs text-destructive hover:underline delete-activity" data-id={a.id}>Hapus</button>
          </li>
        ))}
      </ul>
    )}
  </div>

  <script>
    document.querySelectorAll<HTMLInputElement>('.toggle-activity').forEach((cb) => {
      cb.addEventListener('change', async () => {
        await fetch(`/api/admin/activities/${cb.dataset.id}`, {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ is_completed: cb.checked })
        })
        window.location.reload()
      })
    })
    document.querySelectorAll<HTMLButtonElement>('.delete-activity').forEach((btn) => {
      btn.addEventListener('click', async () => {
        if (!confirm('Hapus aktivitas ini?')) return
        await fetch(`/api/admin/activities/${btn.dataset.id}`, { method: 'DELETE' })
        window.location.reload()
      })
    })
  </script>
</AdminLayout>
```

- [ ] **Step 4:** Add `Activities` to sidebar — import `CheckSquare` icon in `src/pages/admin/layout.astro` and add link after `pipeline`:

```ts
{ key: 'activities', href: '/admin/activities', icon: CheckSquare, label: 'Activities' },
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/admin/activities.ts src/pages/api/admin/activities.ts src/pages/api/admin/activities/ src/pages/admin/activities/ src/pages/admin/layout.astro
git commit -m "feat(admin): activities CRUD with filter views"
```

---

### Task 2.7: Notifications endpoint and bell

**Files:**
- Create: `src/lib/admin/notifications.ts`
- Create: `src/pages/api/admin/notifications.ts`
- Create: `src/components/admin/NotificationBell.astro`
- Modify: `src/pages/admin/layout.astro` — embed bell

- [ ] **Step 1: Helper**

Write `src/lib/admin/notifications.ts`:

```ts
import { getSupabase } from '../supabase'

export interface NotificationCounts {
  newOrders: number
  pendingPayments: number
  newConsultations: number
  overdueActivities: number
  total: number
}

export async function getNotificationCounts(sinceISO: string | null): Promise<NotificationCounts> {
  const supabase = getSupabase(true)
  if (!supabase) {
    return { newOrders: 0, pendingPayments: 0, newConsultations: 0, overdueActivities: 0, total: 0 }
  }
  const since = sinceISO ?? new Date(Date.now() - 24 * 3600_000).toISOString()
  const nowISO = new Date().toISOString()

  const [orders, payments, cons, acts] = await Promise.all([
    supabase.from('orders').select('id', { count: 'exact', head: true }).gte('created_at', since),
    supabase.from('payments').select('id', { count: 'exact', head: true }).in('status', ['pending', 'processing']),
    supabase.from('consultations').select('id', { count: 'exact', head: true }).gte('created_at', since),
    supabase.from('activities').select('id', { count: 'exact', head: true }).eq('is_completed', false).lt('due_date', nowISO)
  ])

  const newOrders = orders.count ?? 0
  const pendingPayments = payments.count ?? 0
  const newConsultations = cons.count ?? 0
  const overdueActivities = acts.count ?? 0
  return {
    newOrders,
    pendingPayments,
    newConsultations,
    overdueActivities,
    total: newOrders + pendingPayments + newConsultations + overdueActivities
  }
}
```

- [ ] **Step 2: API route**

Write `src/pages/api/admin/notifications.ts`:

```ts
import type { APIRoute } from 'astro'
import { getNotificationCounts } from '../../../lib/admin/notifications'
import { requireAdmin } from '../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const since = new URL(ctx.request.url).searchParams.get('since')
  const counts = await getNotificationCounts(since)
  return new Response(JSON.stringify(counts), { status: 200, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 3: Bell component**

Write `src/components/admin/NotificationBell.astro`:

```astro
---
import { Bell } from 'lucide-astro'
---
<div class="relative" id="notif-root">
  <button id="notif-btn" class="h-10 w-10 rounded-full glass-panel flex items-center justify-center hover:bg-accent transition relative" aria-label="Notifications">
    <Bell class="h-4 w-4" />
    <span id="notif-dot" class="absolute top-1 right-1 h-2.5 w-2.5 rounded-full bg-red-500 hidden"></span>
  </button>
  <div id="notif-panel" class="hidden absolute right-0 mt-2 w-72 rounded-xl glass-panel border border-border shadow-lg p-3 z-50">
    <h3 class="text-sm font-semibold mb-2">Notifikasi</h3>
    <div id="notif-list" class="space-y-1 text-sm"></div>
  </div>
</div>

<script>
  const dot = document.getElementById('notif-dot')!
  const list = document.getElementById('notif-list')!
  const btn = document.getElementById('notif-btn')!
  const panel = document.getElementById('notif-panel')!

  btn.addEventListener('click', () => panel.classList.toggle('hidden'))
  document.addEventListener('click', (e) => {
    if (!(e.target as Node).closest?.('#notif-root')) panel.classList.add('hidden')
  })

  async function poll() {
    const since = localStorage.getItem('admin_last_seen') ?? new Date(Date.now() - 24 * 3600_000).toISOString()
    try {
      const res = await fetch(`/api/admin/notifications?since=${encodeURIComponent(since)}`)
      if (!res.ok) return
      const c = await res.json()
      dot.classList.toggle('hidden', c.total === 0)
      const items: Array<[string, number, string]> = [
        ['Order baru', c.newOrders, '/admin/orders'],
        ['Payment pending', c.pendingPayments, '/admin/payments'],
        ['Konsultasi baru', c.newConsultations, '/admin/consultations'],
        ['Activities terlambat', c.overdueActivities, '/admin/activities?filter=overdue']
      ]
      list.innerHTML = items
        .filter(([, count]) => count > 0)
        .map(([label, count, href]) => `<a href="${href}" class="flex justify-between rounded-md px-2 py-1.5 hover:bg-accent/40"><span>${label}</span><span class="font-bold">${count}</span></a>`)
        .join('') || '<p class="text-xs text-muted-foreground">Tidak ada notifikasi baru.</p>'
    } catch {}
  }

  poll()
  setInterval(poll, 30000)

  // mark seen on tab visit reset on full reload of any admin page
  localStorage.setItem('admin_last_seen', new Date().toISOString())
</script>
```

- [ ] **Step 4:** Embed bell in layout. Edit `src/pages/admin/layout.astro` — find the top-bar `flex items-center gap-3` block on the right and add before "Online" pill:

```astro
import NotificationBell from '../../components/admin/NotificationBell.astro'
// ...
<div class="flex items-center gap-3">
  <NotificationBell />
  <div class="hidden sm:flex items-center gap-2 px-3 py-1.5 rounded-full glass-pill">
    ...
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/admin/notifications.ts src/pages/api/admin/notifications.ts src/components/admin/NotificationBell.astro src/pages/admin/layout.astro
git commit -m "feat(admin): in-dashboard notifications with 30s polling"
```

---

### Task 2.8: Phase 2 verification

- [ ] **Step 1:** `bun run astro check` → 0 errors
- [ ] **Step 2:** `bun run test` → all PASS
- [ ] **Step 3:** Smoke test:
  - Create a lead via Pipeline page modal
  - Drag the lead between columns; verify stage updates persist after reload
  - Create an activity tied to a lead
  - Toggle activity completion
  - Verify notification bell shows counts when there are pending payments / overdue activities
- [ ] **Step 4:** Tag milestone: `git tag phase-2-pipeline`

---

## PHASE 3 — Analytics & Reports

> Spec reference: §2 Phase 3, §5.8 Reports, §6.3 API routes

### Task 3.1: Analytics helper module + tests

**Files:**
- Create: `src/lib/admin/analytics.ts`
- Test: `src/__tests__/admin-analytics.test.ts`

- [ ] **Step 1: Write test for date bucket builder**

```ts
import { describe, it, expect } from 'vitest'
import { buildDateBuckets, type Granularity } from '../lib/admin/analytics'

describe('analytics buildDateBuckets', () => {
  it('builds N daily buckets ending today', () => {
    const buckets = buildDateBuckets('2026-05-01', '2026-05-05', 'day')
    expect(buckets.length).toBe(5)
    expect(buckets[0]).toBe('2026-05-01')
    expect(buckets[4]).toBe('2026-05-05')
  })

  it('builds weekly buckets', () => {
    const buckets = buildDateBuckets('2026-04-01', '2026-04-30', 'week')
    expect(buckets.length).toBeGreaterThan(0)
    expect(buckets.every((b) => /^\d{4}-W\d{2}$/.test(b))).toBe(true)
  })

  it('builds monthly buckets', () => {
    const buckets = buildDateBuckets('2026-01-01', '2026-04-30', 'month')
    expect(buckets).toEqual(['2026-01', '2026-02', '2026-03', '2026-04'])
  })
})
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement helper**

Write `src/lib/admin/analytics.ts`:

```ts
import { getSupabase } from '../supabase'

export type Granularity = 'day' | 'week' | 'month'

export interface DataPoint {
  bucket: string
  value: number
  count?: number
}

function bucketFor(dateISO: string, gran: Granularity): string {
  const d = new Date(dateISO)
  if (gran === 'day') return d.toISOString().slice(0, 10)
  if (gran === 'month') return `${d.getUTCFullYear()}-${String(d.getUTCMonth() + 1).padStart(2, '0')}`
  // week: ISO week
  const tmp = new Date(Date.UTC(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate()))
  const dayNum = tmp.getUTCDay() || 7
  tmp.setUTCDate(tmp.getUTCDate() + 4 - dayNum)
  const yearStart = new Date(Date.UTC(tmp.getUTCFullYear(), 0, 1))
  const week = Math.ceil(((tmp.getTime() - yearStart.getTime()) / 86400000 + 1) / 7)
  return `${tmp.getUTCFullYear()}-W${String(week).padStart(2, '0')}`
}

export function buildDateBuckets(fromISO: string, toISO: string, gran: Granularity): string[] {
  const from = new Date(fromISO)
  const to = new Date(toISO)
  const out: string[] = []
  const seen = new Set<string>()
  const cursor = new Date(from)
  while (cursor <= to) {
    const b = bucketFor(cursor.toISOString(), gran)
    if (!seen.has(b)) {
      out.push(b)
      seen.add(b)
    }
    cursor.setUTCDate(cursor.getUTCDate() + 1)
  }
  return out
}

export interface RevenueParams {
  from: string
  to: string
  granularity: Granularity
}

export async function revenueSeries(params: RevenueParams): Promise<DataPoint[]> {
  const supabase = getSupabase(true)
  if (!supabase) return []
  const { data } = await supabase
    .from('payments')
    .select('amount, paid_at')
    .eq('status', 'paid')
    .gte('paid_at', params.from)
    .lte('paid_at', params.to)

  const buckets = buildDateBuckets(params.from, params.to, params.granularity)
  const map = new Map<string, number>(buckets.map((b) => [b, 0]))
  for (const p of data ?? []) {
    if (!p.paid_at) continue
    const key = bucketFor(p.paid_at, params.granularity)
    map.set(key, (map.get(key) ?? 0) + Number(p.amount))
  }
  return buckets.map((b) => ({ bucket: b, value: map.get(b) ?? 0 }))
}

export interface FunnelStage {
  name: string
  count: number
}

export async function funnelStats(fromISO: string, toISO: string): Promise<FunnelStage[]> {
  const supabase = getSupabase(true)
  if (!supabase) return []

  const [leads, orders, paid] = await Promise.all([
    supabase.from('leads').select('id', { count: 'exact', head: true }).gte('created_at', fromISO).lte('created_at', toISO),
    supabase.from('orders').select('id', { count: 'exact', head: true }).gte('created_at', fromISO).lte('created_at', toISO),
    supabase.from('payments').select('id', { count: 'exact', head: true }).eq('status', 'paid').gte('paid_at', fromISO).lte('paid_at', toISO)
  ])

  return [
    { name: 'Leads', count: leads.count ?? 0 },
    { name: 'Orders', count: orders.count ?? 0 },
    { name: 'Paid', count: paid.count ?? 0 }
  ]
}

export async function breakdownByService(fromISO: string, toISO: string): Promise<Array<{ name: string; orders: number; revenue: number }>> {
  const supabase = getSupabase(true)
  if (!supabase) return []
  const { data } = await supabase
    .from('orders')
    .select('service_type, pricing')
    .gte('created_at', fromISO)
    .lte('created_at', toISO)

  const m = new Map<string, { orders: number; revenue: number }>()
  for (const o of data ?? []) {
    const cur = m.get(o.service_type) ?? { orders: 0, revenue: 0 }
    cur.orders += 1
    cur.revenue += Number((o.pricing as any)?.grand_total ?? 0)
    m.set(o.service_type, cur)
  }
  return Array.from(m.entries()).map(([name, v]) => ({ name, ...v })).sort((a, b) => b.revenue - a.revenue)
}

export async function breakdownByLeadSource(fromISO: string, toISO: string): Promise<Array<{ source: string; count: number }>> {
  const supabase = getSupabase(true)
  if (!supabase) return []
  const { data } = await supabase
    .from('leads')
    .select('source')
    .gte('created_at', fromISO)
    .lte('created_at', toISO)

  const m = new Map<string, number>()
  for (const r of data ?? []) m.set(r.source, (m.get(r.source) ?? 0) + 1)
  return Array.from(m.entries()).map(([source, count]) => ({ source, count })).sort((a, b) => b.count - a.count)
}
```

- [ ] **Step 4: Run test, expect PASS, commit**

```bash
bun run test src/__tests__/admin-analytics.test.ts
git add src/lib/admin/analytics.ts src/__tests__/admin-analytics.test.ts
git commit -m "feat(admin): analytics helpers for revenue, funnel, breakdowns"
```

---

### Task 3.2: Reports API routes

**Files:**
- Create: `src/pages/api/admin/reports/revenue.ts`
- Create: `src/pages/api/admin/reports/funnel.ts`
- Create: `src/pages/api/admin/reports/breakdown.ts`
- Create: `src/pages/api/admin/reports/export.ts`

- [ ] **Step 1: Revenue endpoint**

Write `src/pages/api/admin/reports/revenue.ts`:

```ts
import type { APIRoute } from 'astro'
import { revenueSeries } from '../../../../lib/admin/analytics'
import { requireAdmin } from '../../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const url = new URL(ctx.request.url)
  const from = url.searchParams.get('from') ?? new Date(Date.now() - 30 * 86400_000).toISOString().slice(0, 10)
  const to = url.searchParams.get('to') ?? new Date().toISOString().slice(0, 10)
  const granularity = (url.searchParams.get('granularity') as any) ?? 'day'
  const series = await revenueSeries({ from, to, granularity })
  return new Response(JSON.stringify({ series }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 2: Funnel endpoint**

Write `src/pages/api/admin/reports/funnel.ts`:

```ts
import type { APIRoute } from 'astro'
import { funnelStats } from '../../../../lib/admin/analytics'
import { requireAdmin } from '../../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const url = new URL(ctx.request.url)
  const from = url.searchParams.get('from') ?? new Date(Date.now() - 30 * 86400_000).toISOString()
  const to = url.searchParams.get('to') ?? new Date().toISOString()
  const stages = await funnelStats(from, to)
  return new Response(JSON.stringify({ stages }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 3: Breakdown endpoint**

Write `src/pages/api/admin/reports/breakdown.ts`:

```ts
import type { APIRoute } from 'astro'
import { breakdownByService, breakdownByLeadSource } from '../../../../lib/admin/analytics'
import { requireAdmin } from '../../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const url = new URL(ctx.request.url)
  const type = url.searchParams.get('type') ?? 'service'
  const from = url.searchParams.get('from') ?? new Date(Date.now() - 30 * 86400_000).toISOString()
  const to = url.searchParams.get('to') ?? new Date().toISOString()
  if (type === 'source') {
    const data = await breakdownByLeadSource(from, to)
    return new Response(JSON.stringify({ data }), { status: 200, headers: { 'Content-Type': 'application/json' } })
  }
  const data = await breakdownByService(from, to)
  return new Response(JSON.stringify({ data }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 4: CSV export endpoint**

Write `src/pages/api/admin/reports/export.ts`:

```ts
import type { APIRoute } from 'astro'
import { getSupabase } from '../../../../lib/supabase'
import { requireAdmin } from '../../../../lib/admin/auth'

function csvEscape(v: unknown): string {
  const s = v === null || v === undefined ? '' : String(v)
  if (s.includes(',') || s.includes('"') || s.includes('\n')) {
    return `"${s.replace(/"/g, '""')}"`
  }
  return s
}

export const GET: APIRoute = async (ctx) => {
  const guard = await requireAdmin(ctx)
  if (guard instanceof Response) return guard
  const supabase = getSupabase(true)
  if (!supabase) return new Response('Server not configured', { status: 500 })

  const url = new URL(ctx.request.url)
  const type = url.searchParams.get('type') ?? 'orders'
  const from = url.searchParams.get('from') ?? new Date(Date.now() - 30 * 86400_000).toISOString()
  const to = url.searchParams.get('to') ?? new Date().toISOString()

  if (type === 'orders') {
    const { data } = await supabase.from('orders').select('*').gte('created_at', from).lte('created_at', to)
    const rows = data ?? []
    const headers = ['id', 'created_at', 'customer_name', 'whatsapp', 'email', 'company', 'service_type', 'status', 'grand_total']
    const lines = [headers.join(',')]
    for (const r of rows) {
      lines.push([
        r.id, r.created_at, r.customer_name, r.whatsapp, r.email, r.company,
        r.service_type, r.status, (r.pricing as any)?.grand_total ?? 0
      ].map(csvEscape).join(','))
    }
    return new Response(lines.join('\n'), {
      status: 200,
      headers: {
        'Content-Type': 'text/csv; charset=utf-8',
        'Content-Disposition': `attachment; filename="orders-${from.slice(0,10)}-to-${to.slice(0,10)}.csv"`
      }
    })
  }

  if (type === 'payments') {
    const { data } = await supabase.from('payments').select('*').gte('created_at', from).lte('created_at', to)
    const rows = data ?? []
    const headers = ['id', 'created_at', 'paid_at', 'order_id', 'gateway', 'payment_type', 'status', 'amount']
    const lines = [headers.join(',')]
    for (const r of rows) {
      lines.push([r.id, r.created_at, r.paid_at, r.order_id, r.gateway, r.payment_type, r.status, r.amount].map(csvEscape).join(','))
    }
    return new Response(lines.join('\n'), {
      status: 200,
      headers: {
        'Content-Type': 'text/csv; charset=utf-8',
        'Content-Disposition': `attachment; filename="payments-${from.slice(0,10)}-to-${to.slice(0,10)}.csv"`
      }
    })
  }

  return new Response('Unsupported type', { status: 400 })
}
```

- [ ] **Step 5: Commit**

```bash
git add src/pages/api/admin/reports/
git commit -m "feat(admin): reports API for revenue, funnel, breakdown, CSV export"
```

---

### Task 3.3: Reports page

**Files:**
- Create: `src/components/admin/FunnelChart.astro`
- Create: `src/pages/admin/reports/index.astro`

- [ ] **Step 1: Funnel chart component**

Write `src/components/admin/FunnelChart.astro`:

```astro
---
interface Props {
  stages: Array<{ name: string; count: number }>
}
const { stages } = Astro.props
const max = Math.max(...stages.map((s) => s.count), 1)
---
<div class="space-y-2">
  {stages.map((s, i) => {
    const width = Math.round((s.count / max) * 100)
    const conv = i > 0 ? Math.round((s.count / Math.max(stages[i - 1].count, 1)) * 100) : 100
    return (
      <div>
        <div class="flex items-center justify-between text-xs mb-1">
          <span class="font-medium">{s.name}</span>
          <span class="text-muted-foreground">{s.count} {i > 0 && <>· {conv}% konversi</>}</span>
        </div>
        <div class="h-8 rounded-md bg-muted/30 overflow-hidden">
          <div class="h-full bg-primary/80" style={`width:${width}%`}></div>
        </div>
      </div>
    )
  })}
</div>
```

- [ ] **Step 2: Reports page**

Write `src/pages/admin/reports/index.astro`:

```astro
---
import AdminLayout from '../layout.astro'
import RevenueChart from '../../../components/admin/RevenueChart.astro'
import FunnelChart from '../../../components/admin/FunnelChart.astro'
import { revenueSeries, funnelStats, breakdownByService, breakdownByLeadSource, type Granularity } from '../../../lib/admin/analytics'
import { Download } from 'lucide-astro'

const url = new URL(Astro.request.url)
const today = new Date().toISOString().slice(0, 10)
const monthAgo = new Date(Date.now() - 30 * 86400_000).toISOString().slice(0, 10)
const from = url.searchParams.get('from') ?? monthAgo
const to = url.searchParams.get('to') ?? today
const granularity = (url.searchParams.get('granularity') ?? 'day') as Granularity

const [series, stages, services, sources] = await Promise.all([
  revenueSeries({ from, to, granularity }),
  funnelStats(from, to),
  breakdownByService(from, to),
  breakdownByLeadSource(from, to)
])

const seriesForChart = series.map((p) => ({ day: p.bucket, value: p.value }))
const fmt = (n: number) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n)
---
<AdminLayout title="Reports">
  <form method="GET" action="/admin/reports" class="glass-card rounded-2xl p-4 mb-6 flex flex-wrap gap-3 items-end">
    <label class="block">
      <span class="text-xs text-muted-foreground">Dari</span>
      <input type="date" name="from" value={from} class="block mt-1 rounded-lg border border-border bg-background px-2 py-1 text-sm" />
    </label>
    <label class="block">
      <span class="text-xs text-muted-foreground">Sampai</span>
      <input type="date" name="to" value={to} class="block mt-1 rounded-lg border border-border bg-background px-2 py-1 text-sm" />
    </label>
    <label class="block">
      <span class="text-xs text-muted-foreground">Granularitas</span>
      <select name="granularity" class="block mt-1 rounded-lg border border-border bg-background px-2 py-1 text-sm">
        <option value="day" selected={granularity === 'day'}>Harian</option>
        <option value="week" selected={granularity === 'week'}>Mingguan</option>
        <option value="month" selected={granularity === 'month'}>Bulanan</option>
      </select>
    </label>
    <button type="submit" class="rounded-lg bg-primary px-4 py-2 text-sm text-primary-foreground">Terapkan</button>
    <a href={`/api/admin/reports/export?type=orders&from=${from}&to=${to}`} class="ml-auto inline-flex items-center gap-1 rounded-lg border border-border px-4 py-2 text-sm hover:bg-accent/40">
      <Download class="h-4 w-4" /> Export Orders CSV
    </a>
    <a href={`/api/admin/reports/export?type=payments&from=${from}&to=${to}`} class="inline-flex items-center gap-1 rounded-lg border border-border px-4 py-2 text-sm hover:bg-accent/40">
      <Download class="h-4 w-4" /> Export Payments CSV
    </a>
  </form>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-6">
    <div class="lg:col-span-2 glass-card rounded-2xl p-6">
      <h2 class="text-lg font-semibold mb-4">Revenue</h2>
      <RevenueChart series={seriesForChart} height={260} />
    </div>
    <div class="glass-card rounded-2xl p-6">
      <h2 class="text-lg font-semibold mb-4">Conversion Funnel</h2>
      <FunnelChart stages={stages} />
    </div>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <div class="glass-card rounded-2xl p-6">
      <h2 class="text-lg font-semibold mb-4">Breakdown per Layanan</h2>
      <table class="w-full text-sm">
        <thead><tr class="text-left text-xs text-muted-foreground border-b border-border"><th class="pb-2">Layanan</th><th class="pb-2 text-right">Order</th><th class="pb-2 text-right">Revenue</th></tr></thead>
        <tbody>
          {services.map((s) => (
            <tr class="border-b border-border/40">
              <td class="py-2">{s.name}</td>
              <td class="py-2 text-right">{s.orders}</td>
              <td class="py-2 text-right font-medium">{fmt(s.revenue)}</td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
    <div class="glass-card rounded-2xl p-6">
      <h2 class="text-lg font-semibold mb-4">Sumber Lead</h2>
      <table class="w-full text-sm">
        <thead><tr class="text-left text-xs text-muted-foreground border-b border-border"><th class="pb-2">Source</th><th class="pb-2 text-right">Jumlah</th></tr></thead>
        <tbody>
          {sources.map((s) => (
            <tr class="border-b border-border/40"><td class="py-2">{s.source}</td><td class="py-2 text-right font-medium">{s.count}</td></tr>
          ))}
        </tbody>
      </table>
    </div>
  </div>
</AdminLayout>
```

- [ ] **Step 3:** Add `Reports` to sidebar — import `BarChart3` icon in `src/pages/admin/layout.astro`:

```ts
{ key: 'reports', href: '/admin/reports', icon: BarChart3, label: 'Reports' },
```

(Place after `consultations`.)

- [ ] **Step 4: Smoke test** — visit `/admin/reports`, change date range, click CSV export, verify file downloads.

- [ ] **Step 5: Commit**

```bash
git add src/components/admin/FunnelChart.astro src/pages/admin/reports/ src/pages/admin/layout.astro
git commit -m "feat(admin): reports page with revenue chart, funnel, breakdowns, CSV export"
```

---

### Task 3.4: Phase 3 verification

- [ ] **Step 1:** `bun run astro check`, `bun run test` — pass
- [ ] **Step 2:** Smoke test all chart variations (day/week/month)
- [ ] **Step 3:** Tag: `git tag phase-3-reports`

---

## PHASE 4 — Content & System

> Spec reference: §2 Phase 4, §5.9 Blog, §5.10 Vouchers, §5.11 Audit Logs

### Task 4.1: Blog helper module + tests

**Files:**
- Create: `src/lib/admin/blog.ts`
- Test: `src/__tests__/admin-blog.test.ts`

- [ ] **Step 1: Test the slug helper**

```ts
import { describe, it, expect } from 'vitest'
import { slugify } from '../lib/admin/blog'

describe('blog slugify', () => {
  it('lowercases and replaces spaces with dashes', () => {
    expect(slugify('Cara Membuat Website')).toBe('cara-membuat-website')
  })
  it('strips diacritics', () => {
    expect(slugify('Tegàl ínformatika')).toBe('tegal-informatika')
  })
  it('removes punctuation', () => {
    expect(slugify('Tips & Trick: SEO!')).toBe('tips-trick-seo')
  })
  it('collapses multiple dashes', () => {
    expect(slugify('a---b')).toBe('a-b')
  })
})
```

- [ ] **Step 2: Run, expect FAIL**

- [ ] **Step 3: Implement helper**

Write `src/lib/admin/blog.ts`:

```ts
import { marked } from 'marked'
import { getSupabase } from '../supabase'
import type { BlogPost, BlogPostInsert, BlogPostUpdate } from '../../types/database'

export function slugify(input: string): string {
  return input
    .normalize('NFKD')
    .replace(/[̀-ͯ]/g, '')
    .toLowerCase()
    .replace(/[^a-z0-9\s-]/g, '')
    .trim()
    .replace(/\s+/g, '-')
    .replace(/-+/g, '-')
}

export interface ListPostsParams {
  locale?: string
  status?: 'draft' | 'published' | 'archived'
  search?: string
  page?: number
  pageSize?: number
}

export async function listPosts(params: ListPostsParams = {}): Promise<{ posts: BlogPost[]; total: number; totalPages: number; page: number }> {
  const { locale, status, search, page = 1, pageSize = 20 } = params
  const supabase = getSupabase(true)
  if (!supabase) return { posts: [], total: 0, totalPages: 0, page }
  const from = (page - 1) * pageSize
  const to = from + pageSize - 1
  let q = supabase.from('blog_posts').select('*', { count: 'exact' }).order('updated_at', { ascending: false }).range(from, to)
  if (locale) q = q.eq('locale', locale)
  if (status) q = q.eq('status', status)
  if (search) q = q.ilike('title', `%${search}%`)
  const { data, count } = await q
  const total = count ?? 0
  return {
    posts: (data ?? []) as BlogPost[],
    total,
    totalPages: Math.max(1, Math.ceil(total / pageSize)),
    page
  }
}

export async function getPost(id: string): Promise<BlogPost | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  const { data } = await supabase.from('blog_posts').select('*').eq('id', id).single()
  return (data as BlogPost) ?? null
}

export async function createPost(payload: BlogPostInsert & { body_md?: string }): Promise<BlogPost | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  const body_html = payload.body_md ? await marked.parse(payload.body_md) : (payload.body_html ?? null)
  const { body_md, ...rest } = payload
  const { data } = await supabase.from('blog_posts').insert({ ...rest, body_html }).select().single()
  return (data as BlogPost) ?? null
}

export async function updatePost(id: string, payload: BlogPostUpdate & { body_md?: string }): Promise<BlogPost | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  const update = { ...payload }
  if (payload.body_md) {
    update.body_html = await marked.parse(payload.body_md)
  }
  delete (update as any).body_md
  const { data } = await supabase.from('blog_posts').update(update).eq('id', id).select().single()
  return (data as BlogPost) ?? null
}

export async function deletePost(id: string): Promise<boolean> {
  const supabase = getSupabase(true)
  if (!supabase) return false
  const { error } = await supabase.from('blog_posts').delete().eq('id', id)
  return !error
}
```

- [ ] **Step 4: PASS, commit**

```bash
bun run test src/__tests__/admin-blog.test.ts
git add src/lib/admin/blog.ts src/__tests__/admin-blog.test.ts
git commit -m "feat(admin): blog helper with markdown→HTML conversion"
```

---

### Task 4.2: Blog API routes

**Files:**
- Create: `src/pages/api/admin/blog.ts`
- Create: `src/pages/api/admin/blog/[id].ts`

- [ ] **Step 1: List/create endpoint**

Write `src/pages/api/admin/blog.ts`:

```ts
import type { APIRoute } from 'astro'
import { createPost, listPosts } from '../../../lib/admin/blog'
import { requireRole } from '../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin', 'editor'])
  if (guard instanceof Response) return guard
  const url = new URL(ctx.request.url)
  const result = await listPosts({
    locale: url.searchParams.get('locale') ?? undefined,
    status: (url.searchParams.get('status') as any) ?? undefined,
    search: url.searchParams.get('search') ?? undefined,
    page: Number(url.searchParams.get('page') ?? '1') || 1
  })
  return new Response(JSON.stringify(result), { status: 200, headers: { 'Content-Type': 'application/json' } })
}

export const POST: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin', 'editor'])
  if (guard instanceof Response) return guard
  const body = await ctx.request.json()
  if (!body.title || !body.slug || !body.locale) {
    return new Response(JSON.stringify({ error: 'title, slug, locale required' }), { status: 400 })
  }
  const post = await createPost({
    title: body.title,
    slug: body.slug,
    locale: body.locale,
    description: body.description ?? null,
    body_md: body.body_md,
    body_html: body.body_html ?? null,
    category: body.category ?? 'Web Development',
    tags: body.tags ?? [],
    author: body.author ?? 'Tim Ekalliptus',
    image: body.image ?? null,
    image_alt: body.image_alt ?? null,
    featured: body.featured ?? false,
    seo_meta_title: body.seo_meta_title ?? null,
    seo_meta_description: body.seo_meta_description ?? null,
    seo_noindex: body.seo_noindex ?? false,
    status: body.status ?? 'draft',
    publish_date: body.publish_date ?? null
  })
  if (!post) return new Response(JSON.stringify({ error: 'failed' }), { status: 500 })
  return new Response(JSON.stringify({ post }), { status: 201, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 2: Single-post endpoint**

Write `src/pages/api/admin/blog/[id].ts`:

```ts
import type { APIRoute } from 'astro'
import { deletePost, getPost, updatePost } from '../../../../lib/admin/blog'
import { requireRole } from '../../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin', 'editor'])
  if (guard instanceof Response) return guard
  const post = await getPost(ctx.params.id as string)
  if (!post) return new Response(JSON.stringify({ error: 'Not found' }), { status: 404 })
  return new Response(JSON.stringify({ post }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}

export const PATCH: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin', 'editor'])
  if (guard instanceof Response) return guard
  const body = await ctx.request.json()
  const updated = await updatePost(ctx.params.id as string, body)
  if (!updated) return new Response(JSON.stringify({ error: 'failed' }), { status: 500 })
  return new Response(JSON.stringify({ post: updated }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}

export const DELETE: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin', 'editor'])
  if (guard instanceof Response) return guard
  const ok = await deletePost(ctx.params.id as string)
  return new Response(JSON.stringify({ success: ok }), { status: ok ? 200 : 500 })
}
```

- [ ] **Step 3: Commit**

```bash
git add src/pages/api/admin/blog.ts src/pages/api/admin/blog/[id].ts
git commit -m "feat(admin): blog CRUD API with role guard"
```

---

### Task 4.3: Blog list and editor pages

**Files:**
- Create: `src/pages/admin/blog/index.astro`
- Create: `src/pages/admin/blog/new.astro`
- Create: `src/pages/admin/blog/[id].astro`

- [ ] **Step 1: List page**

Write `src/pages/admin/blog/index.astro`:

```astro
---
import AdminLayout from '../layout.astro'
import SearchFilter from '../../../components/admin/SearchFilter.astro'
import Pagination from '../../../components/admin/Pagination.astro'
import EmptyState from '../../../components/admin/EmptyState.astro'
import { FileText, Plus } from 'lucide-astro'
import { listPosts } from '../../../lib/admin/blog'

const url = new URL(Astro.request.url)
const search = url.searchParams.get('search') ?? ''
const status = url.searchParams.get('status') ?? ''
const locale = url.searchParams.get('locale') ?? ''
const page = Math.max(1, Number(url.searchParams.get('page') ?? '1'))

const { posts, total, totalPages } = await listPosts({
  search: search || undefined,
  status: (status || undefined) as any,
  locale: locale || undefined,
  page
})
const baseUrl = `/admin/blog?search=${encodeURIComponent(search)}&status=${status}&locale=${locale}`
---
<AdminLayout title="Blog">
  <div class="flex justify-between items-center mb-4">
    <h2 class="text-lg font-semibold">{total} post</h2>
    <a href="/admin/blog/new" class="inline-flex items-center gap-1 rounded-xl bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90">
      <Plus class="h-4 w-4" /> Tulis Post Baru
    </a>
  </div>

  <SearchFilter
    placeholder="Cari judul..."
    value={search}
    name="search"
    action="/admin/blog"
    filters={[
      { name: 'status', label: 'Status', selected: status, options: [
        { value: '', label: 'Semua Status' },
        { value: 'draft', label: 'Draft' },
        { value: 'published', label: 'Published' },
        { value: 'archived', label: 'Archived' }
      ]},
      { name: 'locale', label: 'Bahasa', selected: locale, options: [
        { value: '', label: 'Semua Bahasa' },
        { value: 'id', label: 'Indonesia' },
        { value: 'en', label: 'English' },
        { value: 'ja', label: '日本語' },
        { value: 'ko', label: '한국어' },
        { value: 'ru', label: 'Русский' },
        { value: 'ar', label: 'العربية' },
        { value: 'tr', label: 'Türkçe' }
      ]}
    ]}
  />

  <div class="glass-card rounded-2xl p-6">
    {posts.length === 0 ? (
      <EmptyState icon={FileText} title="Belum ada post" ctaLabel="Tulis Post Pertama" ctaHref="/admin/blog/new" />
    ) : (
      <ul class="divide-y divide-border/40">
        {posts.map((p) => (
          <li class="flex items-center justify-between py-3">
            <div>
              <a href={`/admin/blog/${p.id}`} class="font-medium hover:text-primary">{p.title}</a>
              <div class="text-xs text-muted-foreground">{p.category} · {p.locale} · diperbarui {new Date(p.updated_at).toLocaleDateString('id-ID')}</div>
            </div>
            <span class:list={[
              'rounded-full px-2 py-0.5 text-xs',
              p.status === 'published' ? 'bg-green-500/15 text-green-600' : p.status === 'draft' ? 'bg-muted text-muted-foreground' : 'bg-orange-500/15 text-orange-500'
            ]}>{p.status}</span>
          </li>
        ))}
      </ul>
    )}
    <Pagination page={page} totalPages={totalPages} baseUrl={baseUrl} />
  </div>
</AdminLayout>
```

- [ ] **Step 2: New post page**

Write `src/pages/admin/blog/new.astro`:

```astro
---
import AdminLayout from '../layout.astro'
---
<AdminLayout title="Post Baru">
  <form id="new-post-form" class="glass-card rounded-2xl p-6 max-w-3xl space-y-4">
    <div class="grid grid-cols-2 gap-3">
      <label class="block">
        <span class="text-xs text-muted-foreground">Judul</span>
        <input name="title" required class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </label>
      <label class="block">
        <span class="text-xs text-muted-foreground">Slug</span>
        <input name="slug" required class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </label>
    </div>
    <div class="grid grid-cols-3 gap-3">
      <label class="block">
        <span class="text-xs text-muted-foreground">Bahasa</span>
        <select name="locale" required class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm">
          <option value="id">Indonesia</option><option value="en">English</option><option value="ja">日本語</option>
          <option value="ko">한국어</option><option value="ru">Русский</option><option value="ar">العربية</option><option value="tr">Türkçe</option>
        </select>
      </label>
      <label class="block">
        <span class="text-xs text-muted-foreground">Kategori</span>
        <input name="category" class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </label>
      <label class="block">
        <span class="text-xs text-muted-foreground">Tags (pisah koma)</span>
        <input name="tags" class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </label>
    </div>
    <label class="block">
      <span class="text-xs text-muted-foreground">Deskripsi (meta)</span>
      <textarea name="description" rows="2" class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm"></textarea>
    </label>
    <label class="block">
      <span class="text-xs text-muted-foreground">Body (Markdown)</span>
      <textarea name="body_md" rows="14" class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm font-mono"></textarea>
    </label>
    <div class="grid grid-cols-3 gap-3">
      <label class="block">
        <span class="text-xs text-muted-foreground">Image URL</span>
        <input name="image" class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </label>
      <label class="block">
        <span class="text-xs text-muted-foreground">Image Alt</span>
        <input name="image_alt" class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </label>
      <label class="block">
        <span class="text-xs text-muted-foreground">Status</span>
        <select name="status" class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm">
          <option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option>
        </select>
      </label>
    </div>
    <label class="flex items-center gap-2 text-sm">
      <input type="checkbox" name="featured" /> Featured
    </label>
    <div class="flex justify-end gap-2">
      <a href="/admin/blog" class="rounded-lg border border-border px-4 py-2 text-sm">Batal</a>
      <button type="submit" class="rounded-lg bg-primary px-4 py-2 text-sm text-primary-foreground">Simpan</button>
    </div>
  </form>

  <script>
    const form = document.getElementById('new-post-form') as HTMLFormElement
    form.addEventListener('submit', async (e) => {
      e.preventDefault()
      const fd = new FormData(form)
      const data: any = Object.fromEntries(fd)
      data.tags = (data.tags ?? '').split(',').map((s: string) => s.trim()).filter(Boolean)
      data.featured = !!fd.get('featured')
      const res = await fetch('/api/admin/blog', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) })
      if (res.ok) {
        const { post } = await res.json()
        window.location.href = `/admin/blog/${post.id}`
      } else {
        const { error } = await res.json().catch(() => ({ error: 'gagal' }))
        alert(`Gagal: ${error}`)
      }
    })
  </script>
</AdminLayout>
```

- [ ] **Step 3: Edit page**

Write `src/pages/admin/blog/[id].astro`:

```astro
---
import AdminLayout from '../layout.astro'
import { getPost } from '../../../lib/admin/blog'
import { Trash2 } from 'lucide-astro'

const id = Astro.params.id as string
const post = await getPost(id)
if (!post) return Astro.redirect('/admin/blog')
---
<AdminLayout title={post.title}>
  <form id="edit-post-form" class="glass-card rounded-2xl p-6 max-w-3xl space-y-4" data-id={id}>
    <div class="grid grid-cols-2 gap-3">
      <label class="block">
        <span class="text-xs text-muted-foreground">Judul</span>
        <input name="title" value={post.title} required class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </label>
      <label class="block">
        <span class="text-xs text-muted-foreground">Slug</span>
        <input name="slug" value={post.slug} required class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </label>
    </div>
    <label class="block">
      <span class="text-xs text-muted-foreground">Body (Markdown / HTML)</span>
      <textarea name="body_md" rows="14" class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm font-mono"></textarea>
    </label>
    <p class="text-xs text-muted-foreground">HTML saat ini: {post.body_html?.length ?? 0} karakter</p>
    <div class="grid grid-cols-2 gap-3">
      <label class="block">
        <span class="text-xs text-muted-foreground">Status</span>
        <select name="status" class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm">
          <option value="draft" selected={post.status === 'draft'}>Draft</option>
          <option value="published" selected={post.status === 'published'}>Published</option>
          <option value="archived" selected={post.status === 'archived'}>Archived</option>
        </select>
      </label>
      <label class="block">
        <span class="text-xs text-muted-foreground">Kategori</span>
        <input name="category" value={post.category ?? ''} class="mt-1 block w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </label>
    </div>
    <div class="flex justify-between gap-2">
      <button type="button" id="delete-btn" class="inline-flex items-center gap-1 rounded-lg border border-destructive text-destructive px-4 py-2 text-sm hover:bg-destructive/10"><Trash2 class="h-4 w-4" /> Hapus</button>
      <div class="flex gap-2">
        <a href="/admin/blog" class="rounded-lg border border-border px-4 py-2 text-sm">Batal</a>
        <button type="submit" class="rounded-lg bg-primary px-4 py-2 text-sm text-primary-foreground">Simpan</button>
      </div>
    </div>
  </form>

  <script>
    const form = document.getElementById('edit-post-form') as HTMLFormElement
    const id = form.dataset.id!
    form.addEventListener('submit', async (e) => {
      e.preventDefault()
      const fd = new FormData(form)
      const data: any = Object.fromEntries(fd)
      const res = await fetch(`/api/admin/blog/${id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) })
      if (res.ok) window.location.href = '/admin/blog'
      else alert('Gagal menyimpan')
    })
    document.getElementById('delete-btn')!.addEventListener('click', async () => {
      if (!confirm('Hapus post ini?')) return
      const res = await fetch(`/api/admin/blog/${id}`, { method: 'DELETE' })
      if (res.ok) window.location.href = '/admin/blog'
    })
  </script>
</AdminLayout>
```

- [ ] **Step 4: Add Blog to sidebar** in `src/pages/admin/layout.astro`. Import `FileText`:

```ts
{ key: 'blog', href: '/admin/blog', icon: FileText, label: 'Blog' },
```

- [ ] **Step 5: Commit**

```bash
git add src/pages/admin/blog/ src/pages/admin/layout.astro
git commit -m "feat(admin): blog list, create, edit pages with markdown"
```

---

### Task 4.4: Voucher management

**Files:**
- Create: `src/lib/admin/vouchers.ts`
- Create: `src/pages/api/admin/vouchers.ts`
- Create: `src/pages/api/admin/vouchers/[code].ts`
- Create: `src/pages/admin/vouchers/index.astro`

- [ ] **Step 1: Helper**

Write `src/lib/admin/vouchers.ts`:

```ts
import { getSupabase } from '../supabase'
import type { Voucher, VoucherInsert } from '../../types/database'

export async function listVouchers(): Promise<Voucher[]> {
  const supabase = getSupabase(true)
  if (!supabase) return []
  const { data } = await supabase.from('vouchers').select('*').order('created_at', { ascending: false })
  return (data ?? []) as Voucher[]
}

export async function createVoucher(payload: VoucherInsert): Promise<Voucher | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  const { data } = await supabase.from('vouchers').insert({ ...payload, code: payload.code.toUpperCase() }).select().single()
  return (data as Voucher) ?? null
}

export async function updateVoucher(code: string, payload: Partial<VoucherInsert>): Promise<Voucher | null> {
  const supabase = getSupabase(true)
  if (!supabase) return null
  const { data } = await supabase.from('vouchers').update(payload).eq('code', code.toUpperCase()).select().single()
  return (data as Voucher) ?? null
}

export async function deleteVoucher(code: string): Promise<boolean> {
  const supabase = getSupabase(true)
  if (!supabase) return false
  const { error } = await supabase.from('vouchers').delete().eq('code', code.toUpperCase())
  return !error
}
```

- [ ] **Step 2: API**

Write `src/pages/api/admin/vouchers.ts`:

```ts
import type { APIRoute } from 'astro'
import { createVoucher, listVouchers } from '../../../lib/admin/vouchers'
import { requireRole } from '../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin', 'finance'])
  if (guard instanceof Response) return guard
  const vouchers = await listVouchers()
  return new Response(JSON.stringify({ vouchers }), { status: 200, headers: { 'Content-Type': 'application/json' } })
}

export const POST: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin', 'finance'])
  if (guard instanceof Response) return guard
  const body = await ctx.request.json()
  if (!body.code || !body.type || body.value === undefined) {
    return new Response(JSON.stringify({ error: 'code, type, value required' }), { status: 400 })
  }
  const voucher = await createVoucher({
    code: body.code,
    type: body.type,
    value: Number(body.value),
    min_spend: body.min_spend ?? null,
    max_uses: body.max_uses ?? null,
    valid_until: body.valid_until ?? null,
    is_active: body.is_active ?? true
  })
  if (!voucher) return new Response(JSON.stringify({ error: 'failed' }), { status: 500 })
  return new Response(JSON.stringify({ voucher }), { status: 201 })
}
```

Write `src/pages/api/admin/vouchers/[code].ts`:

```ts
import type { APIRoute } from 'astro'
import { deleteVoucher, updateVoucher } from '../../../../lib/admin/vouchers'
import { requireRole } from '../../../../lib/admin/auth'

export const PATCH: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin', 'finance'])
  if (guard instanceof Response) return guard
  const body = await ctx.request.json()
  const updated = await updateVoucher(ctx.params.code as string, body)
  if (!updated) return new Response(JSON.stringify({ error: 'failed' }), { status: 500 })
  return new Response(JSON.stringify({ voucher: updated }), { status: 200 })
}

export const DELETE: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin', 'finance'])
  if (guard instanceof Response) return guard
  const ok = await deleteVoucher(ctx.params.code as string)
  return new Response(JSON.stringify({ success: ok }), { status: ok ? 200 : 500 })
}
```

- [ ] **Step 3: Page**

Write `src/pages/admin/vouchers/index.astro`:

```astro
---
import AdminLayout from '../layout.astro'
import EmptyState from '../../../components/admin/EmptyState.astro'
import { Ticket, Plus } from 'lucide-astro'
import { listVouchers } from '../../../lib/admin/vouchers'

const vouchers = await listVouchers()
const fmt = (n: number) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(n)
---
<AdminLayout title="Vouchers">
  <div class="flex justify-between items-center mb-4">
    <h2 class="text-lg font-semibold">{vouchers.length} voucher</h2>
    <button id="open-new" class="inline-flex items-center gap-1 rounded-xl bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90">
      <Plus class="h-4 w-4" /> Voucher Baru
    </button>
  </div>

  <div class="glass-card rounded-2xl p-6">
    {vouchers.length === 0 ? (
      <EmptyState icon={Ticket} title="Belum ada voucher" />
    ) : (
      <table class="w-full text-sm">
        <thead><tr class="text-left text-xs text-muted-foreground border-b border-border">
          <th class="pb-2">Kode</th><th class="pb-2">Tipe</th><th class="pb-2">Nilai</th><th class="pb-2">Min Spend</th><th class="pb-2">Pakai</th><th class="pb-2">Berlaku Sampai</th><th class="pb-2">Status</th><th class="pb-2"></th>
        </tr></thead>
        <tbody>
          {vouchers.map((v) => (
            <tr class="border-b border-border/40">
              <td class="py-2 font-mono font-bold">{v.code}</td>
              <td class="py-2">{v.type}</td>
              <td class="py-2">{v.type === 'percent' ? `${v.value}%` : fmt(Number(v.value))}</td>
              <td class="py-2">{v.min_spend ? fmt(Number(v.min_spend)) : '-'}</td>
              <td class="py-2">{v.used_count}{v.max_uses ? `/${v.max_uses}` : ''}</td>
              <td class="py-2 text-xs">{v.valid_until ? new Date(v.valid_until).toLocaleDateString('id-ID') : '-'}</td>
              <td class="py-2"><span class:list={['text-xs px-2 py-0.5 rounded-full', v.is_active ? 'bg-green-500/15 text-green-600' : 'bg-muted text-muted-foreground']}>{v.is_active ? 'Aktif' : 'Nonaktif'}</span></td>
              <td class="py-2 text-right space-x-2">
                <button class="text-xs text-primary hover:underline toggle-btn" data-code={v.code} data-active={String(v.is_active)}>{v.is_active ? 'Disable' : 'Enable'}</button>
                <button class="text-xs text-destructive hover:underline delete-btn" data-code={v.code}>Hapus</button>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    )}
  </div>

  <dialog id="new-voucher" class="rounded-2xl p-0 backdrop:bg-black/50">
    <form id="new-form" class="bg-background p-6 w-[420px] max-w-[90vw]">
      <h2 class="text-lg font-semibold mb-4">Voucher Baru</h2>
      <div class="space-y-3">
        <input name="code" required placeholder="Kode (HEMAT10)" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm uppercase" />
        <select name="type" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm">
          <option value="percent">Persen (%)</option>
          <option value="nominal">Nominal (Rp)</option>
        </select>
        <input name="value" type="number" required placeholder="Nilai" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        <input name="min_spend" type="number" placeholder="Min spend (Rp, opsional)" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        <input name="max_uses" type="number" placeholder="Max pemakaian (opsional)" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        <input name="valid_until" type="datetime-local" class="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
      </div>
      <div class="mt-4 flex justify-end gap-2">
        <button type="button" id="cancel-new" class="rounded-lg border border-border px-4 py-2 text-sm">Batal</button>
        <button type="submit" class="rounded-lg bg-primary px-4 py-2 text-sm text-primary-foreground">Simpan</button>
      </div>
    </form>
  </dialog>

  <script>
    const dialog = document.getElementById('new-voucher') as HTMLDialogElement
    document.getElementById('open-new')!.addEventListener('click', () => dialog.showModal())
    document.getElementById('cancel-new')!.addEventListener('click', () => dialog.close())
    document.getElementById('new-form')!.addEventListener('submit', async (e) => {
      e.preventDefault()
      const fd = new FormData(e.target as HTMLFormElement)
      const data: any = Object.fromEntries(fd)
      data.value = Number(data.value)
      if (data.min_spend) data.min_spend = Number(data.min_spend)
      if (data.max_uses) data.max_uses = Number(data.max_uses)
      const res = await fetch('/api/admin/vouchers', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) })
      if (res.ok) window.location.reload()
      else alert('Gagal membuat voucher')
    })
    document.querySelectorAll<HTMLButtonElement>('.toggle-btn').forEach((b) => {
      b.addEventListener('click', async () => {
        const isActive = b.dataset.active === 'true'
        await fetch(`/api/admin/vouchers/${b.dataset.code}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ is_active: !isActive }) })
        window.location.reload()
      })
    })
    document.querySelectorAll<HTMLButtonElement>('.delete-btn').forEach((b) => {
      b.addEventListener('click', async () => {
        if (!confirm(`Hapus voucher ${b.dataset.code}?`)) return
        await fetch(`/api/admin/vouchers/${b.dataset.code}`, { method: 'DELETE' })
        window.location.reload()
      })
    })
  </script>
</AdminLayout>
```

- [ ] **Step 4:** Add Vouchers to sidebar — import `Ticket`:

```ts
{ key: 'vouchers', href: '/admin/vouchers', icon: Ticket, label: 'Vouchers' },
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/admin/vouchers.ts src/pages/api/admin/vouchers.ts src/pages/api/admin/vouchers/ src/pages/admin/vouchers/ src/pages/admin/layout.astro
git commit -m "feat(admin): voucher management with create/toggle/delete"
```

---

### Task 4.5: Audit log viewer + write-on-mutation helper

**Files:**
- Create: `src/lib/admin/audit.ts`
- Create: `src/pages/api/admin/audit-logs.ts`
- Create: `src/pages/admin/audit-logs/index.astro`
- Modify: existing admin mutation routes (orders/leads/blog/vouchers) to write audit log

- [ ] **Step 1: Audit helper**

Write `src/lib/admin/audit.ts`:

```ts
import { getSupabase } from '../supabase'

export interface AuditEntry {
  user_id?: string | null
  action: string
  table_name: string
  record_id?: string | null
  old_values?: any
  new_values?: any
  ip_address?: string | null
  user_agent?: string | null
}

export async function writeAudit(entry: AuditEntry): Promise<void> {
  const supabase = getSupabase(true)
  if (!supabase) return
  await supabase.from('audit_logs').insert({
    action: entry.action,
    table_name: entry.table_name,
    record_id: entry.record_id ?? null,
    old_values: entry.old_values ?? null,
    new_values: entry.new_values ?? null,
    user_id: entry.user_id ?? null,
    ip_address: (entry.ip_address as any) ?? '0.0.0.0',
    user_agent: entry.user_agent ?? null
  })
}

export interface ListAuditParams {
  table?: string
  action?: string
  from?: string
  to?: string
  page?: number
}

export async function listAudit(params: ListAuditParams = {}): Promise<{ rows: any[]; total: number; totalPages: number; page: number }> {
  const supabase = getSupabase(true)
  if (!supabase) return { rows: [], total: 0, totalPages: 0, page: 1 }
  const page = params.page ?? 1
  const pageSize = 30
  const from = (page - 1) * pageSize
  const to = from + pageSize - 1
  let q = supabase.from('audit_logs').select('*', { count: 'exact' }).order('created_at', { ascending: false }).range(from, to)
  if (params.table) q = q.eq('table_name', params.table)
  if (params.action) q = q.eq('action', params.action)
  if (params.from) q = q.gte('created_at', params.from)
  if (params.to) q = q.lte('created_at', params.to)
  const { data, count } = await q
  return { rows: data ?? [], total: count ?? 0, totalPages: Math.max(1, Math.ceil((count ?? 0) / pageSize)), page }
}
```

- [ ] **Step 2: Audit list API**

Write `src/pages/api/admin/audit-logs.ts`:

```ts
import type { APIRoute } from 'astro'
import { listAudit } from '../../../lib/admin/audit'
import { requireRole } from '../../../lib/admin/auth'

export const GET: APIRoute = async (ctx) => {
  const guard = await requireRole(ctx, ['admin'])
  if (guard instanceof Response) return guard
  const url = new URL(ctx.request.url)
  const result = await listAudit({
    table: url.searchParams.get('table') ?? undefined,
    action: url.searchParams.get('action') ?? undefined,
    from: url.searchParams.get('from') ?? undefined,
    to: url.searchParams.get('to') ?? undefined,
    page: Number(url.searchParams.get('page') ?? '1') || 1
  })
  return new Response(JSON.stringify(result), { status: 200, headers: { 'Content-Type': 'application/json' } })
}
```

- [ ] **Step 3: Audit page**

Write `src/pages/admin/audit-logs/index.astro`:

```astro
---
import AdminLayout from '../layout.astro'
import Pagination from '../../../components/admin/Pagination.astro'
import EmptyState from '../../../components/admin/EmptyState.astro'
import { History } from 'lucide-astro'
import { listAudit } from '../../../lib/admin/audit'

const url = new URL(Astro.request.url)
const table = url.searchParams.get('table') ?? ''
const action = url.searchParams.get('action') ?? ''
const page = Math.max(1, Number(url.searchParams.get('page') ?? '1'))

const { rows, total, totalPages } = await listAudit({ table: table || undefined, action: action || undefined, page })
const baseUrl = `/admin/audit-logs?table=${table}&action=${action}`
---
<AdminLayout title="Audit Logs">
  <form method="GET" action="/admin/audit-logs" class="glass-card rounded-2xl p-4 mb-6 flex gap-3 flex-wrap">
    <select name="table" class="rounded-lg border border-border bg-background px-2 py-1 text-sm">
      <option value="">Semua tabel</option>
      <option value="orders" selected={table === 'orders'}>orders</option>
      <option value="payments" selected={table === 'payments'}>payments</option>
      <option value="leads" selected={table === 'leads'}>leads</option>
      <option value="activities" selected={table === 'activities'}>activities</option>
      <option value="blog_posts" selected={table === 'blog_posts'}>blog_posts</option>
      <option value="vouchers" selected={table === 'vouchers'}>vouchers</option>
    </select>
    <select name="action" class="rounded-lg border border-border bg-background px-2 py-1 text-sm">
      <option value="">Semua aksi</option>
      <option value="create" selected={action === 'create'}>create</option>
      <option value="update" selected={action === 'update'}>update</option>
      <option value="delete" selected={action === 'delete'}>delete</option>
    </select>
    <button type="submit" class="rounded-lg bg-primary px-4 py-1 text-sm text-primary-foreground">Filter</button>
  </form>

  <div class="glass-card rounded-2xl p-6">
    {rows.length === 0 ? (
      <EmptyState icon={History} title="Belum ada audit log" />
    ) : (
      <ul class="divide-y divide-border/40">
        {rows.map((r) => (
          <li class="py-3 text-sm">
            <div class="flex items-center justify-between mb-1">
              <span class="font-medium">{r.action} · <span class="text-muted-foreground">{r.table_name}</span></span>
              <span class="text-xs text-muted-foreground">{new Date(r.created_at).toLocaleString('id-ID')}</span>
            </div>
            {r.record_id && <div class="text-xs text-muted-foreground font-mono">id: {r.record_id}</div>}
            {r.new_values && (
              <details class="mt-1">
                <summary class="text-xs cursor-pointer text-primary">Lihat perubahan</summary>
                <pre class="text-xs bg-muted/30 rounded-md p-2 mt-1 overflow-x-auto">{JSON.stringify({ old: r.old_values, new: r.new_values }, null, 2)}</pre>
              </details>
            )}
          </li>
        ))}
      </ul>
    )}
    <Pagination page={page} totalPages={totalPages} baseUrl={baseUrl} />
  </div>
</AdminLayout>
```

- [ ] **Step 4: Wire audit calls into existing mutation routes**

For each of the following files, after a successful mutation call `writeAudit(...)` with the action and entity. Example diff for `src/pages/api/admin/leads/[id].ts` PATCH handler — add at the top of the file:

```ts
import { writeAudit } from '../../../../lib/admin/audit'
```

After `const updated = await updateLead(...)` in PATCH:

```ts
if (updated) {
  await writeAudit({
    user_id: guard.user.id,
    action: 'update',
    table_name: 'leads',
    record_id: updated.id,
    old_values: current,
    new_values: updated,
    ip_address: ctx.clientAddress,
    user_agent: ctx.request.headers.get('user-agent')
  })
}
```

Apply the same pattern to:
- `src/pages/api/admin/leads.ts` (POST → action `create`)
- `src/pages/api/admin/leads/[id].ts` (DELETE → action `delete`)
- `src/pages/api/admin/activities.ts` (POST)
- `src/pages/api/admin/activities/[id].ts` (PATCH, DELETE)
- `src/pages/api/admin/blog.ts` (POST)
- `src/pages/api/admin/blog/[id].ts` (PATCH, DELETE)
- `src/pages/api/admin/vouchers.ts` (POST)
- `src/pages/api/admin/vouchers/[code].ts` (PATCH, DELETE)

For each: read the previous state when the action is update or delete, then call `writeAudit` after the mutation succeeds.

- [ ] **Step 5:** Add Audit Logs to sidebar — import `History`:

```ts
{ key: 'audit', href: '/admin/audit-logs', icon: History, label: 'Audit Logs' },
```

- [ ] **Step 6: Commit**

```bash
git add src/lib/admin/audit.ts src/pages/api/admin/audit-logs.ts src/pages/admin/audit-logs/ src/pages/admin/layout.astro src/pages/api/admin/leads.ts src/pages/api/admin/leads/ src/pages/api/admin/activities.ts src/pages/api/admin/activities/ src/pages/api/admin/blog.ts src/pages/api/admin/blog/ src/pages/api/admin/vouchers.ts src/pages/api/admin/vouchers/
git commit -m "feat(admin): audit log viewer and write-on-mutation"
```

---

### Task 4.6: Multi-role permission enforcement

**Files:**
- Modify: middleware to enforce per-route role visibility
- Modify: `src/pages/admin/layout.astro` — hide menu items based on role

- [ ] **Step 1: Update middleware** to check role for restricted pages.

Edit `src/middleware.ts` after the `getAdminSession` block:

```ts
const ROLE_RULES: Array<{ pattern: RegExp; allowed: string[] }> = [
  { pattern: /^\/admin\/blog/, allowed: ['owner', 'admin', 'editor'] },
  { pattern: /^\/admin\/vouchers/, allowed: ['owner', 'admin', 'finance'] },
  { pattern: /^\/admin\/audit-logs/, allowed: ['owner', 'admin'] },
  { pattern: /^\/admin\/payments/, allowed: ['owner', 'admin', 'finance'] },
  { pattern: /^\/admin\/reports/, allowed: ['owner', 'admin', 'finance'] },
  { pattern: /^\/admin\/settings/, allowed: ['owner', 'admin'] }
]

for (const rule of ROLE_RULES) {
  if (rule.pattern.test(url.pathname)) {
    if (session.role !== 'owner' && !rule.allowed.includes(session.role)) {
      return new Response('Forbidden', { status: 403 })
    }
    break
  }
}

ctx.locals.adminSession = session
return next()
```

(Replace the existing `ctx.locals.adminSession = session; return next()` lines with the block above.)

- [ ] **Step 2: Hide menu items in layout**

Edit `src/pages/admin/layout.astro`. After importing `sidebarLinks`, add filtering:

```ts
const session = Astro.locals.adminSession
const role = session?.role ?? 'admin'

function canSee(key: string): boolean {
  if (role === 'owner' || role === 'admin') return true
  if (key === 'blog') return role === 'editor'
  if (key === 'vouchers' || key === 'payments' || key === 'reports') return role === 'finance'
  if (key === 'consultations') return role === 'cs'
  if (key === 'orders' || key === 'customers' || key === 'pipeline' || key === 'activities' || key === 'dashboard') return true
  return false
}

const visibleLinks = sidebarLinks.filter((l) => canSee(l.key))
```

Then change the rendering loops to use `visibleLinks` instead of `sidebarLinks`.

Also update the user profile block to show the real name and role:

```astro
<p class="text-sm font-medium truncate">{session?.displayName ?? 'Admin User'}</p>
<p class="text-xs text-muted-foreground truncate">{session?.user.email ?? ''} · {role}</p>
```

- [ ] **Step 3: Commit**

```bash
git add src/middleware.ts src/pages/admin/layout.astro
git commit -m "feat(admin): role-based access for restricted pages"
```

---

### Task 4.7: Phase 4 verification + final tag

- [ ] **Step 1:** `bun run astro check`, `bun run test` — all pass
- [ ] **Step 2:** Smoke test:
  - Create a blog post, switch status to published, verify it appears in `/blog` public page
  - Create a voucher, validate it via the existing voucher form on `/order` page
  - Make a mutation (create lead, edit blog), verify entry appears in `/admin/audit-logs`
  - Log in as a `cs`-role profile (manually set in `profiles` table) and verify Blog/Vouchers/Audit pages are hidden in sidebar and return 403 if visited directly
- [ ] **Step 3:** Tag final milestone:

```bash
git tag phase-4-content
```

---

## Self-Review Checklist

After all 4 phases are implemented, verify:

- [ ] `bun run test` passes — at least 5 unit test files (`admin-auth`, `middleware`, `admin-customers`, `admin-leads`, `admin-analytics`, `admin-blog`)
- [ ] `bun run astro check` returns 0 errors
- [ ] All sidebar links in `src/pages/admin/layout.astro` resolve to working pages
- [ ] Logging out clears the cookie and redirects appropriately
- [ ] Visiting `/admin/customers` while logged out redirects to `/admin/login?next=/admin/customers`
- [ ] CSV exports download with correct filenames and valid contents
- [ ] No file in `src/pages/admin/**` contains hardcoded mock arrays for stats/orders/payments
- [ ] All new tables (`leads`, `activities`) have RLS enabled
- [ ] No `console.log` debug statements left behind in admin pages
- [ ] All API routes call either `requireAdmin` or `requireRole`

---

## Deployment notes

After all phases:
1. In Cloudflare dashboard, set the same Supabase service role key in environment variables.
2. Ensure cookie `Secure` flag works on production HTTPS (already in code).
3. Run the SQL migration from Task 2.1 on the production Supabase project.
4. Create at least one user in Supabase Auth and a matching `profiles` row with `role = 'owner'`.
